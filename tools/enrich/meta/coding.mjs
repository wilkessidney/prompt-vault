// coding 全量 meta（由 prompts/coding 反向重建，条目与 md 一一对应）
export default [
  {
    sub: 'architecture',
    slug: 'api-design-restful',
    title: 'RESTful API 设计评审',
    summary: '按资源建模、HTTP 语义、错误规范、版本策略四层评审 API 设计。',
    tags: ['API', 'REST', '设计评审'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是资深 API 设计师。评审并改进下方 API 设计。

## 设计输入
- 业务场景：{{业务场景}}
- 现有设计（可留空）：{{现有 API 清单}}

## 评审清单
1. **资源建模**：URL 是名词还是动词？有无过度嵌套？分页/过滤/排序参数是否统一？
2. **HTTP 语义**：GET 是否幂等？POST/PUT/PATCH 选择是否正确？状态码是否准确（不要 200 包一切）？
3. **错误规范**：错误体格式（code/message/details/request_id）是否统一？
4. **版本与兼容**：破坏性变更策略（URL 版本 / Header 版本 / 字段只增不删）。

## 输出
- 问题表格：位置 | 问题 | 严重度 | 修改建议
- 修改后的完整 API 清单（方法 + 路径 + 简述）
- 3 个最容易踩的坑

**硬规则**：每个建议必须给「为什么」；不确定的业务假设用【假设】标注而不是编造。`,
  },
  {
    sub: 'architecture',
    slug: 'domain-modeling',
    title: '领域建模',
    summary: '从需求提炼聚合根、实体与值对象',
    tags: ['架构', 'DDD', '领域建模', '聚合'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位 DDD 实践者。请对以下业务进行领域建模：
{{业务描述 + 关键规则}}

要求：
- 识别聚合根、实体、值对象，并给出边界理由
- 列出 2-3 个核心聚合及其不变量（invariant）
- 标出跨聚合的一致性问题与最终一致性处理
- 给出仓储（Repository）边界建议
- 用一段伪代码或类图说明聚合间如何协作`,
  },
  {
    sub: 'architecture',
    slug: 'event-driven-design',
    title: '事件驱动架构设计',
    summary: '设计基于事件流的系统边界与消息契约',
    tags: ['架构', '事件驱动', '消息队列', '系统设计'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位资深架构师。请为下面场景设计事件驱动架构：
{{业务场景 + 主要领域 + 吞吐 / 一致性要求}}

要求：
- 画出核心事件流（生产者 → 事件 → 消费者），标注异步边界
- 定义 2-3 个关键事件的 payload schema（字段 + 类型 + 语义）
- 说明如何保证至少一次投递 / 幂等消费
- 指出何时该用事件而非直接调用（解耦 / 审计 / 削峰）
- 给出引入的事件总线选型建议（Kafka / 云原生 / 轻量）及理由`,
  },
  {
    sub: 'architecture',
    slug: 'microservice-split',
    title: '单体拆微服务评估',
    summary: '评估单体应用是否该拆、怎么拆，输出拆分边界与迁移路径。',
    tags: ['微服务', '单体', '演进'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是架构顾问。评估下方单体应用是否值得拆微服务。

## 现状
- 业务域：{{业务描述}}
- 代码规模与模块：{{模块清单}}
- 痛点：{{部署慢 / 团队互相阻塞 / 局部扩容难 / 其他}}
- 团队数：{{团队数}}

## 输出
### 1. 先问该不该拆
对照「拆分收益 vs 引入成本（网络延迟、分布式事务、运维复杂度、可观测性投入）」，给出明确判断：拆 / 不拆 / 拆一部分。如果是「拆一部分」，指出哪部分值得。

### 2. 拆分边界
按 DDD 限界上下文给出服务清单：服务名 | 职责 | 对外接口 | 数据所有权 | 拆分优先级。

### 3. 迁移路径
绞杀者模式分几步走，每步的验证标准。禁止「一次性重写」。

### 4. 不拆的替代方案
模块化单体 + 独立部署能否解决 80% 痛点？

**硬规则**：团队少于 {{团队规模}} 人时默认不推荐全量拆分，除非有压倒性理由。`,
  },
  {
    sub: 'architecture',
    slug: 'performance-bottleneck-plan',
    title: '性能瓶颈排查方案',
    summary: '按「测量→定位→修复→验证」流程生成性能问题排查方案。',
    tags: ['性能优化', '排查'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `帮我生成 {{系统/页面/接口}} 的性能排查与优化方案。

## 现象
- 症状：{{慢在哪里：接口 P99 / 页面加载 / CPU / 内存}}
- 量化数据：{{具体数值，如 P99 从 200ms 涨到 3s}}
- 技术栈：{{技术栈}}
- 流量特征：{{QPS / 数据量}}

## 输出
### 1. 假设清单
按「可能性 × 验证成本」排序，列出至少 6 个瓶颈假设（含数据库慢查询、N+1、缓存未命中、锁竞争、GC、网络 RTT 等，按技术栈裁剪）。

### 2. 测量手段
每个假设对应一条可执行的验证命令或脚本（EXPLAIN、火焰图、链路追踪、日志聚合）。

### 3. 修复优先级
速赢（1天内）→ 中期（1周）→ 长期，各自预期收益估算。

### 4. 防回归
上线后用什么指标监控、设什么告警阈值。

**硬规则**：禁止未测量就给优化结论；每条修复必须绑定它对应的假设编号。`,
  },
  {
    sub: 'architecture',
    slug: 'scalability-review',
    title: '可扩展性评审',
    summary: '找出系统的水平扩展瓶颈并给方案',
    tags: ['架构', '扩展', '性能', '评审'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位 scalability 专家。请评审下面系统的扩展瓶颈：
{{架构描述 / 关键组件 / 当前负载}}

要求：
- 找出有状态节点、单点、热点数据等扩展卡点
- 按「无状态化 / 缓存 / 分片 / 异步」给出改造优先级
- 估算各方案的成本与收益，给出落地顺序
- 指出哪些瓶颈其实不需要现在解决（避免过度设计）
- 给出一条最低风险的起步改造建议`,
  },
  {
    sub: 'architecture',
    slug: 'system-design-review',
    title: '系统架构方案设计',
    summary: '给定业务约束，输出架构选型、模块划分、数据模型与风险清单。',
    tags: ['架构', '系统设计', '技术选型'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是系统架构师。为下方需求设计架构方案。

## 业务需求
{{业务描述}}

## 关键约束
- 预期规模：{{DAU / QPS / 数据量}}
- 一致性要求：{{强一致 / 最终一致}}
- 延迟要求：{{P99 目标}}
- 团队规模与能力：{{团队规模与能力}}
- 已有基础设施：{{已有基础设施}}
- 上线时间：{{上线时间}}
- 预算约束：{{预算约束}}

## 输出结构
### 1. 一句话架构
用一句话说清整体架构（例如「单体 + 读写分离 + 异步任务队列」）。

### 2. 架构图
用 ASCII 或 Mermaid 画出组件与数据流。

### 3. 模块划分
表格：模块 | 职责 | 技术选型 | 选择理由 | 被替换成本

### 4. 数据模型
核心实体的字段与关系（DDL 或 ER 描述），说明索引设计依据。

### 5. 关键路径
描述最主要的一条请求链路：从入口到落库，标注每一步的耗时预算。

### 6. 风险清单
表格：风险 | 触发条件 | 影响 | 缓解方案 | 发现时机

### 7. 演进路径
V1 怎么做到最简并可用 → 什么信号出现时该演进到 V2 → V2 具体改什么

**硬规则**：
- 不要为了「高可用」引入团队hold不住的组件。技术选型必须匹配团队规模。
- 每一项选型都要给「不选 X 的理由」，证明你权衡过。
- 明确指出这个设计在哪个规模下会崩。`,
  },
  {
    sub: 'architecture',
    slug: 'tech-selection-matrix',
    title: '技术选型对比矩阵',
    summary: '给定候选技术栈，输出多维加权评分矩阵与决策建议。',
    tags: ['技术选型', '决策矩阵'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `帮我在 {{候选A}} 和 {{候选B}}{{候选C（可选，没有则忽略）}} 之间做技术选型。

## 项目背景
- 项目类型：{{Web / 移动 / 数据 / AI 应用}}
- 团队现状：{{人数，已有技术栈，熟悉度}}
- 关键诉求（按优先级）：{{性能 / 生态 / 招人容易度 / 文档 / 长期维护}}
- 时间约束：{{上线时间}}

## 输出
### 1. 评分矩阵
维度 | 权重 | 候选A | 候选B（1-5 分 + 一句话依据）
维度至少含：学习曲线、生态成熟度、性能上限、招聘市场、社区活跃度、与现有栈契合度、踩坑资料丰富度。

### 2. 加权总分与结论
给出明确推荐 + 置信度（高/中/低）。

### 3. 反方观点
扮演最反对这个结论的人，说出他最强的 3 条理由。

### 4. 切换成本
如果 6 个月后要换到另一个，代价是什么？

**硬规则**：评分必须给依据，禁止裸打分。`,
  },
  {
    sub: 'database',
    slug: 'backup-dr-plan',
    title: '备份与容灾方案',
    summary: '设计 RPO/RTO 可达的备份与恢复策略',
    tags: ['数据库', '容灾', '备份', '运维'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位可靠性工程师。请设计数据库备份与容灾方案：
{{数据库类型 + 数据量 + 业务可接受的 RPO/RTO}}

要求：
- 给出备份策略（全量/增量/日志）与保留周期
- 设计主备 / 多可用区 / 跨区域复制拓扑
- 明确恢复演练频率与关键校验点
- 列出恢复 runbook 的核心步骤与责任人
- 估算成本并就「恢复优先级」给出分层建议`,
  },
  {
    sub: 'database',
    slug: 'data-migration-plan',
    title: '数据迁移方案设计',
    summary: '设计零停机数据迁移：双写、校验、回滚、灰度全流程。',
    tags: ['数据迁移', '上线'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `为下方需求设计数据迁移方案。

## 需求
- 迁移内容：{{表 / 库 / 分库分表 / 换存储引擎}}
- 数据量：{{数据量}}
- 停机窗口：{{能否停机 / 最大容忍}}
- 一致性要求：{{强一致 / 最终一致可接受分钟级}}

## 输出
### 1. 迁移架构图
存量怎么搬（全量 + 增量 binlog/cdc）、新写入怎么处理（双写 or 切流）。

### 2. 分阶段执行计划
阶段 | 动作 | 验证标准 | 回滚方式 | 预计耗时。至少含：准备、全量、增量追平、灰度读、灰度写、全量切换、观察期、收尾。

### 3. 数据校验
行数对账 + 抽样字段比对 + 实时 diff 监控的具体做法。

### 4. 风险与预案
每个阶段失败时的止血动作（必须具体到命令级思路）。

**硬规则**：任何阶段都必须可回滚；切换动作必须有 checklist。`,
  },
  {
    sub: 'database',
    slug: 'index-tuning',
    title: '索引调优',
    summary: '根据查询模式设计或修正索引',
    tags: ['数据库', '索引', 'SQL', '性能'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位 DBA。请针对以下查询做索引调优：
{{表结构（DDL）+ 慢查询 + 执行计划要点}}

要求：
- 指出缺失或冗余的索引，给出 CREATE INDEX 建议
- 说明复合索引的最左前缀与列顺序选择
- 提醒索引带来的写入成本与维护代价
- 给出验证方法（EXPLAIN / 覆盖索引命中）
- 若数据量大，建议分区或冷热分离的权衡`,
  },
  {
    sub: 'database',
    slug: 'nosql-selection',
    title: 'NoSQL 选型',
    summary: '按访问模式决定用哪种 NoSQL',
    tags: ['数据库', 'NoSQL', '选型', '架构'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位数据架构师。请为以下场景做 NoSQL 选型：
{{数据形态 + 访问模式 + 规模 + 一致性要求}}

要求：
- 在文档 / 宽列 / KV / 图 / 时序中给出推荐及排除项
- 说明该选型如何匹配访问模式（读多写多 / 聚合查询）
- 指出放弃关系型带来的代价（事务 / 关联 / 迁移）
- 给出数据建模样例（主键 / 分区键设计）
- 推荐具体产品（托管优先）并说明运维负担`,
  },
  {
    sub: 'database',
    slug: 'schema-design-review',
    title: '库表结构设计评审',
    summary: '评审表设计：范式、索引、扩展性、容量预估四维检查。',
    tags: ['数据库', '建模'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `评审下方表结构设计。

## 业务背景
{{业务场景与核心查询模式}}

## 现有 DDL
\`\`\`sql
{{表结构 DDL}}
\`\`\`

## 预期规模
日增 {{日增量}} 行，一年 {{年增量}} 行。

## 评审维度
1. **字段设计**：类型是否最小够用？可空语义是否清晰？金额是否用整型分存储？时间统一 UTC 了吗？
2. **范式与冗余**：哪里该反范式加速读？冗余字段的双写一致性怎么保证？
3. **索引**：对照查询模式逐一检查，缺哪些复合索引？有没有冗余索引？
4. **扩展性**：新增枚举值、软删、多租户、分表键，现在设计留口子了吗？
5. **容量**：按增长速度，单表多少个月后需要分表？

## 输出
问题清单（严重度排序）+ 修订版完整 DDL + 迁移注意事项。`,
  },
  {
    sub: 'database',
    slug: 'sql-from-natural-language',
    title: '自然语言生成 SQL（开发向）',
    summary: '输入表结构和业务问题，生成可执行 SQL 并解释推理。',
    tags: ['SQL', 'NL2SQL'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `根据表结构把业务问题翻译成 SQL。

## 表结构
\`\`\`sql
{{建表语句或表结构描述}}
\`\`\`

## 业务问题
{{用自然语言描述想查什么}}

## 输出
1. **澄清假设**：问题里模糊的地方（时间口径含不含今天？去重怎么算？），列出你的假设，禁止瞎猜。
2. **SQL**：带注释的可执行语句，方言为 {{MySQL / PostgreSQL / SQLite}}。
3. **解释**：逐段说明每个 JOIN / WHERE / 窗口函数在干什么。
4. **性能提醒**：这个查询在大表上会不会慢？需要什么索引？

**硬规则**：如果表结构不足以回答问题，先提问而不是编造字段。`,
  },
  {
    sub: 'database',
    slug: 'sql-optimize',
    title: 'SQL 优化与索引设计',
    summary: '分析慢查询执行计划，给出改写方案与索引建议，并量化预期提升。',
    tags: ['SQL', '索引', '性能'],
    model: '通用',
    level: '进阶',
    body: `你是数据库性能专家（{{MySQL 8 / PostgreSQL 15 / Oracle}}）。

## 慢查询
\`\`\`sql
{{SQL}}
\`\`\`

## 表结构
\`\`\`sql
{{DDL}}
\`\`\`

## 现状
- 表数据量：{{行数}}
- 当前耗时：{{毫秒}}
- 执行计划：
\`\`\`
{{EXPLAIN 输出}}
\`\`\`

## 分析要求
1. **解读执行计划**：逐行说明每一步在做什么，标出全表扫描、临时表、filesort、回表次数。
2. **定位瓶颈**：是没走索引、索引失效、扫描行数过多、还是 JOIN 顺序问题？
3. **给出方案**（按优先级，每条包含）：
   - 改写后的 SQL
   - 建议索引（含字段顺序与理由，遵循最左前缀与区分度排序）
   - 预期扫描行数变化：{{当前}} → {{优化后}}
   - 副作用（写放大、索引体积、锁范围）

## 额外检查
- 是否存在隐式类型转换导致索引失效？
- 是否存在函数包裹列导致索引失效？
- \`SELECT *\` 是否取了不必要的字段导致回表？
- 分页是否可用游标替代 \`OFFSET\`？

## 禁止
不要建议「加个缓存」来掩盖 SQL 本身的问题。先让 SQL 走对。`,
  },
  {
    sub: 'database',
    slug: 'sql-query-optimizer',
    title: '慢 SQL 诊断与优化',
    summary: '输入慢查询与表结构，输出执行计划分析与优化方案。',
    tags: ['SQL', '索引', '优化'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是数据库专家。诊断并优化下方慢查询。

## 输入
- 数据库类型：{{MySQL / PostgreSQL / 其他}}
- 慢 SQL：
\`\`\`sql
{{慢 SQL 语句}}
\`\`\`
- 表结构与行数：{{相关表的 DDL 与行数}}
- 执行计划（如有）：{{EXPLAIN 输出}}

## 输出
1. **执行计划解读**：逐行解释 EXPLAIN，指出全表扫描、filesort、临时表等坏味道。
2. **根因**：为什么慢（索引缺失 / 索引失效写法 / 数据类型隐式转换 / 关联过多）。
3. **优化方案**：改写 SQL + 建索引（给出完整 DDL，说明索引选择依据与写入代价）。
4. **预期收益**：优化前后扫描行数量级对比。
5. **风险**：锁表风险、索引对写入的影响、回滚方案。

**硬规则**：禁止「加个索引就好了」这种没依据的结论；索引建议必须推演最左前缀是否命中。`,
  },
  {
    sub: 'debug',
    slug: 'concurrency-bug-analysis',
    title: '并发问题分析',
    summary: '分析竞态、死锁、可见性等并发 bug，给出复现思路与修复。',
    tags: ['并发', '竞态', '死锁'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是并发专家。分析下方疑似并发问题。

## 现象
- 症状：{{偶发错误数据 / 死锁 / 卡死 / 计数不对}}
- 复现概率：{{复现频率}}
- 并发模型：{{多线程 / 协程 / 多进程 / 分布式}}
- 相关代码：
\`\`\`
{{可疑代码段}}
\`\`\`

## 输出
### 1. 竞态窗口标注
在代码里逐行指出哪个时间窗口可能交错，两种线程的执行序列图。

### 2. 归类
属于哪类：数据竞态 / 死锁（给出锁的持有顺序图）/ 丢失更新 / 可见性 / 幂等缺失。

### 3. 复现方案
怎么用压力测试或协调工具（latch/barrier）把偶发变成必现。

### 4. 修复选项
锁粒度 / 原子操作 / 不可变数据 / 队列串行化，各自的适用条件与代价。

### 5. 验证
修复后怎么证明对了（不是「跑了一晚上没复现」）。`,
  },
  {
    sub: 'debug',
    slug: 'error-triage-checklist',
    title: '报错信息快速分诊',
    summary: '输入报错堆栈，输出去噪解读、可疑点排序与下一步排查动作。',
    tags: ['调试', '报错分析'],
    model: '通用',
    level: '通用',
    body: `帮我分诊下方报错。

## 报错信息
\`\`\`
{{完整报错与堆栈}}
\`\`\`

## 环境
- 语言/框架：{{语言与框架}}
- 触发场景：{{什么操作触发的}}
- 最近改动：{{最近改过什么}}

## 输出
1. **一句话结论**：这个错误本质上是什么（用大白话）。
2. **堆栈去噪**：哪些帧是自己代码、哪些是框架噪音，第一行「属于你的代码」在哪。
3. **可疑点排序**：按可能性列出 3-5 个嫌疑，每个附带「验证它的最快动作」（加日志 / 断点 / 打印变量 / 查文档）。
4. **常见坑**：这类报错的经典成因 top3。
5. **如果都不对**：下一步该收集什么信息。

**硬规则**：不要只复述报错文本；每个嫌疑必须有可执行的验证动作。`,
  },
  {
    sub: 'debug',
    slug: 'flaky-test-debug',
    title: '偶现测试排查',
    summary: '定位并修复 flaky 测试的根因',
    tags: ['调试', '测试', 'flaky', '稳定性'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位测试稳定性专家。请排查下面的偶现失败测试：
{{测试代码 + 失败日志 + 复现概率}}

要求：
- 列出常见 flaky 成因清单（时序 / 异步 / 共享状态 / 时钟）
- 针对症状定位最可能的根因并给出验证手段
- 提供最小修复（重试 / 固定种子 / 隔离 / 等待条件）
- 说明如何避免「用 sleep 掩盖问题」的反模式
- 给出防止回归的防护（并发隔离 / 确定性输入）`,
  },
  {
    sub: 'debug',
    slug: 'log-analysis',
    title: '日志根因分析',
    summary: '从一堆日志里还原故障时间线',
    tags: ['调试', '日志', '排障', '可观测'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位 SRE。请根据以下日志还原故障：
{{日志片段（含时间 / 级别 / 服务）}}

要求：
- 按时间线梳理关键事件与首次异常点
- 区分「根因」与「连锁报错」，不要被表象带偏
- 标出缺失的关键日志（为下次埋点建议）
- 给出最可能的根因假设与验证下一步
- 用一句话总结故障画像，便于同步给团队`,
  },
  {
    sub: 'debug',
    slug: 'memory-leak-trace',
    title: '内存泄漏排查',
    summary: '按对象生命周期追踪法排查内存泄漏，输出定位与修复。',
    tags: ['内存', '泄漏', '性能'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `帮我排查内存泄漏。

## 现象
- 运行时：{{Node / JVM / Python / Go}}
- 症状：{{RSS 持续上涨 / OOM / GC 频繁}}
- 增长曲线：{{多久涨多少}}
- 可疑改动：{{最近新增的功能或依赖}}

## 输出
### 1. 经典嫌疑清单
按该运行时列 top 嫌疑（全局缓存无上限、闭包持有大对象、事件监听器未解绑、定时器泄漏、ClassLoader 泄漏等），每条配「怎么验证」。

### 2. 取证步骤
堆快照怎么打（命令级）、两次快照怎么 diff、怎么按「保留大小」排序找嫌疑对象、怎么从引用链找到泄漏源。

### 3. 修复方案
针对最可能的 1-2 个嫌疑给出代码级修复（LRU 上限、WeakRef、显式清理、解绑）。

### 4. 防回归
内存基线监控 + 阈值告警建议。

**硬规则**：区分「泄漏」和「还没到 GC 时机的高水位」；修复前先给取证方法。`,
  },
  {
    sub: 'debug',
    slug: 'race-condition-finder',
    title: '竞态条件定位',
    summary: '在并发代码里找出数据竞争',
    tags: ['调试', '并发', '竞态', '线程'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位并发专家。请在下面代码里找竞态条件：
{{并发代码（线程 / 协程 / async）}}

要求：
- 标出所有共享可变状态与未加保护的访问点
- 指出哪些路径会触发数据竞争或死锁
- 给出修复（锁粒度 / 无锁 / 不可变 / 串行化队列）
- 说明为何某些「看起来对」的写法仍有窗口
- 提供可复现的最小用例或检测工具（race detector）建议`,
  },
  {
    sub: 'debug',
    slug: 'rootcause-5why',
    title: 'Bug 根因分析（5 Why 追溯）',
    summary: '对线上 bug 做 5 Why 根因追溯，输出修复与流程防复发。',
    tags: ['调试', '根因分析'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `你是故障复盘专家。对下方 bug 做根因分析。

## 故障信息
- 现象：{{报错信息 / 错误行为}}
- 影响范围：{{影响面}}
- 复现条件：{{复现步骤或概率}}
- 相关代码/日志：
\`\`\`
{{代码或日志片段}}
\`\`\`

## 输出
### 1. 时间线
从触发到发现到恢复，按时间排列已知事实（区分「事实」与「推测」）。

### 2. 5 Why 追溯
为什么出错 → 为什么没被测试拦住 → 为什么没被 code review 拦住 → 为什么监控没更早发现 → 为什么流程允许这样。每层给出证据。

### 3. 直接修复
具体到代码级的修改方案。

### 4. 防复发
测试用例、监控告警、流程改进各至少一条。

**硬规则**：推测必须标注【推测】；禁止把「人为失误」当根因——追问是什么系统条件让人容易失误。`,
  },
  {
    sub: 'debug',
    slug: 'systematic-debug',
    title: '系统化排错流程',
    summary: '按假设-验证循环定位 Bug 根因，拒绝「试试看」式修法。',
    tags: ['调试', '排错', '根因分析'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `你是调试专家，遵循「先复现、再定位、最后修复」的顺序。禁止在找到根因前给出修改方案。

## 问题现象
{{现象描述：期望行为 vs 实际行为}}

## 环境
- 语言/框架版本：{{语言/框架版本}}
- 平台：{{平台}}
- 复现频率：{{必现 / 偶发 x% / 仅特定环境}}

## 相关代码
\`\`\`{{语言}}
{{相关代码}}
\`\`\`

## 报错信息
\`\`\`
{{完整堆栈}}
\`\`\`

## 已经尝试过的操作
{{列出，没有就写「无」}}

## 请按以下流程进行
### 第一步：信息缺口
列出定位根因还缺的关键信息（日志、变量值、环境差异），按重要性排序，最多 5 条。
如果缺失信息导致无法定位，就在这里停下，先让我补齐。

### 第二步：假设列表
列出 3-5 个可能根因，按「可能性 × 验证成本」排序。
每个假设写明：假设内容 → 会导致观察到什么现象 → 如何用最小代价证伪。

### 第三步：验证路径
针对排序第一的假设，给出具体的验证步骤（加什么日志、打印什么变量、用什么工具）。

### 第四步：根因与修复
确认根因后：
1. 一句话说清根因（真正的因果链，不是表面现象）
2. 最小修复方案
3. 为什么这个修复是对的
4. 如何加测试防止回归

**硬规则**：不要用「可能是…你可以试试…」开头。每个结论要么有证据支撑，要么明确标注为猜测。`,
  },
  {
    sub: 'devops',
    slug: 'ci-pipeline-design',
    title: 'CI 流水线设计',
    summary: '为项目设计分层 CI 流水线：快反馈门禁 + 慢验证分离。',
    tags: ['CI', '流水线'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `为下方项目设计 CI 流水线。

## 项目
- 语言/框架：{{语言与框架}}
- 代码仓库：{{GitHub / GitLab / 其他}}
- 团队规模：{{团队规模}}
- 当前痛点：{{流水线太慢 / 没有流水线 / 质量门禁缺失}}

## 输出
### 1. 阶段设计
阶段 | 触发条件 | 内容 | 超时 | 失败策略。至少含：lint、单测、构建、集成测试、安全扫描；说明哪些必须分钟级反馈。

### 2. 加速策略
缓存（依赖/构建产物）、并行分片、路径过滤触发、变更感知测试选择——逐条说明本项目适不适用。

### 3. 质量门禁
哪些失败必须阻塞合并（红线），哪些只警告（黄线）。

### 4. 完整配置文件
按 {{GitHub Actions / GitLab CI}} 输出可用的 yaml。

**硬规则**：PR 反馈必须在 10 分钟内；红线只放「真正不能带上线的问题」。`,
  },
  {
    sub: 'devops',
    slug: 'cost-optimization-cloud',
    title: '云成本优化',
    summary: '找出云账单里的浪费并降本',
    tags: ['DevOps', '成本', '云', 'FinOps'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位 FinOps 顾问。请分析以下云资源使用并降本：
{{资源清单 / 用量 / 账单要点}}

要求：
- 找出闲置 / 低利用率资源（空跑实例 / 未挂载盘 / 旧快照）
- 给出规格下调、Spot、预留实例、自动伸缩的组合建议
- 估算每项可省比例与改动风险
- 指出「先别动」的高风险项
- 提供一张按优先级排序的降本行动表`,
  },
  {
    sub: 'devops',
    slug: 'dockerfile-harden',
    title: 'Dockerfile 瘦身与安全加固',
    summary: '多阶段构建、降权限、清缓存，产出体积更小更安全的镜像配置。',
    tags: ['Docker', 'CI/CD', '安全'],
    model: '通用',
    level: '通用',
    body: `你是容器化与供应链安全工程师。优化下面的 Dockerfile。

## 原始 Dockerfile
\`\`\`dockerfile
{{Dockerfile}}
\`\`\`

## 应用类型
{{语言与框架}}

## 优化目标（按优先级）
1. **体积**：多阶段构建，运行阶段只保留运行时依赖；清理包管理器缓存；合并 RUN 层。
2. **安全**：
   - 非 root 用户运行
   - 固定基础镜像 tag 到 digest 或具体版本（不用 latest）
   - 删除构建期密钥与临时文件
   - 不在镜像中留 shell 调试工具（生产镜像）
3. **构建速度**：按变更频率排层，依赖文件先 COPY；合理使用 BuildKit 缓存挂载。
4. **可观测**：正确的 \`HEALTHCHECK\`、信号处理（用 \`exec\` 或 tini 避免 PID 1 僵尸进程问题）。

## 输出
### 优化后 Dockerfile
（完整，带行内注释说明每行为什么这么写）

### 改动对照表
| 项目 | 优化前 | 优化后 | 说明 |

### 预估效果
- 镜像体积：{{原始大小}} → {{预估大小}}
- 构建时间变化说明

### .dockerignore
给出配套的 .dockerignore 文件

### 遗留风险
说明本次优化没解决、需要你确认的问题（如基础镜像 CVE、多架构支持）。`,
  },
  {
    sub: 'devops',
    slug: 'dockerfile-hardening',
    title: 'Dockerfile 优化与加固',
    summary: '重构 Dockerfile：多阶段构建、镜像瘦身、安全基线三合一。',
    tags: ['Docker', '容器'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `优化下方 Dockerfile。

## 现有文件
\`\`\`dockerfile
{{Dockerfile 内容}}
\`\`\`

## 运行环境
- 应用类型：{{应用类型}}
- 目标平台：{{linux/amd64 / arm64}}
- 当前镜像大小（如知）：{{镜像大小}}

## 输出
1. **问题清单**：逐条指出问题（root 运行、latest 标签、层缓存浪费、密钥打入镜像、无健康检查等）。
2. **重构版 Dockerfile**：多阶段构建 + 非 root 用户 + 固定版本 + .dockerignore 配套内容。
3. **体积对比估算**：优化前后量级差异及来源。
4. **安全检查**：还建议跑什么扫描（trivy 等），关注什么类型漏洞。

**硬规则**：apt/apk install 后必须同层清理；禁止把 secret 任何形式写进镜像。`,
  },
  {
    sub: 'devops',
    slug: 'incident-response-runbook',
    title: '线上故障应急手册',
    summary: '生成某服务的应急 runbook：分级、诊断树、止血动作、升级路径。',
    tags: ['运维', '故障处理', 'SOP'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `为 {{服务名}} 生成线上故障应急手册（runbook）。

## 服务信息
- 功能：{{服务做什么}}
- 技术栈与依赖：{{技术栈 + 依赖的中间件}}
- 部署形态：{{部署方式与实例数}}
- 核心指标：{{关键 SLI}}

## 输出
### 1. 故障分级
P0-P3 判定标准（对用户的影响描述，不用内部术语），各级响应时限。

### 2. 快速诊断树
「告警响了 → 先看哪 3 个 dashboard → 分支判断 → 各分支的定位动作」，画成流程图（Mermaid）。

### 3. 止血动作库
场景 | 动作 | 命令/入口 | 风险 | 需要谁的审批。至少覆盖：回滚、扩容、降级开关、限流、重启。

### 4. 升级路径
什么情况找谁（值班表模板），通知模板（对内 + 对用户）。

### 5. 复盘要求
故障后多长时间内产出什么。

**硬规则**：所有止血动作必须「先保用户，再查原因」；每个动作注明风险。`,
  },
  {
    sub: 'devops',
    slug: 'k8s-troubleshooting',
    title: 'K8s 问题排查指南',
    summary: 'Pod 起不来 / OOM / 调度失败等常见 K8s 问题的排查树。',
    tags: ['Kubernetes', '排查'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `帮我排查 Kubernetes 问题。

## 现象
- 症状：{{Pod Pending / CrashLoopBackOff / OOMKilled / Service 不通 / 节点 NotReady}}
- 工作负载类型：{{Deployment / StatefulSet / Job}}
- 已知信息：
\`\`\`
{{kubectl describe / logs 输出}}
\`\`\`

## 输出
### 1. 现象解读
这个状态码本质上说明什么。

### 2. 排查树
按顺序给出 kubectl 命令序列（get pods → describe → events → logs → 资源水位），每个命令看什么信号。

### 3. 按我的具体情况给出
最可能的 3 个原因 + 验证方法 + 修复方案（yaml 级）。

### 4. 常见坑
该症状的 top 坑（如 imagePullPolicy、resource requests 超节点容量、livenessProbe 误杀、DNS 配置）。

**硬规则**：命令必须可直接复制执行；修改生产资源前标注【需确认】。`,
  },
  {
    sub: 'devops',
    slug: 'monitoring-alerting',
    title: '监控与告警设计',
    summary: '设计不误报的告警与核心指标',
    tags: ['DevOps', '监控', '告警', 'SLO'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位可观测性工程师。请设计监控告警方案：
{{系统关键链路 + SLO 目标}}

要求：
- 区分 SLI / SLO / 告警阈值，避免「监控一切」
- 给出 3-5 个黄金指标（延迟 / 错误 / 流量 / 饱和）
- 设计告警分级（page / ticket）与静默 / 去重策略
- 指出易误报指标及其抑制手段
- 提供一份值班响应的最低检查清单`,
  },
  {
    sub: 'devops',
    slug: 'terraform-module',
    title: 'Terraform 模块设计',
    summary: '把基础设施抽象成可复用的 IaC 模块',
    tags: ['DevOps', 'Terraform', 'IaC', '云'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位平台工程师。请设计可复用的 Terraform 模块：
{{要管理的资源 + 使用方诉求}}

要求：
- 定义清晰的 input variables 与 output，避免硬编码
- 给出合理的默认值与可覆盖项
- 说明 state 隔离（workspace / backend）策略
- 标注安全注意点（密钥 / 公开 bucket / 权限）
- 提供最小调用示例与常用变体`,
  },
  {
    sub: 'docs',
    slug: 'adr-architecture-decision',
    title: 'ADR 架构决策记录',
    summary: '把一次技术决策写成 ADR：背景、选项、决策、后果四段式。',
    tags: ['ADR', '架构决策', '文档'],
    model: '通用',
    level: '进阶',
    body: `你是架构决策记录（ADR）撰写助手。把下方决策过程整理成标准 ADR。

## 决策输入
- 决策主题：{{要决定什么}}
- 备选方案：{{方案A}} / {{方案B}}（可更多）
- 决策结果：{{最终选了什么}}
- 决策时间与参与人：{{时间与相关人}}

## 输出（ADR 标准格式）
### 1. 标题与状态
\`ADR-{{编号}}：<一句话决策>\`，状态：{{提议中 / 已接受 / 已废弃（被 ADR-X 替代）}}

### 2. 背景
什么问题逼出了这个决策？不解决会怎样？包含必要的量化信息（规模、痛点频率）。

### 3. 决策驱动因素
按权重排列：哪些约束是硬的（预算/合规/时间），哪些是偏好。

### 4. 考虑过的选项
每个选项：描述 | 优势 | 劣势 | 放弃原因。**包括最终选项**——它也要有劣势清单。

### 5. 决策结果
选了什么、由谁拍板、什么时候生效。

### 6. 后果
- 正面：解决了什么
- 负面：付出了什么代价、新增了什么风险
- 中性：带来哪些后续工作

**硬规则**：ADR 记录的是「当时的判断」，禁止事后诸葛亮式美化；劣势必须如实写，这是 ADR 最大的价值。`,
  },
  {
    sub: 'docs',
    slug: 'api-doc-writer',
    title: 'API 文档撰写',
    summary: '把接口定义转成开发者一看就会用的 API 文档。',
    tags: ['API', '文档'],
    model: '通用',
    level: '通用',
    body: `为下方接口写 API 文档。

## 接口信息
- 接口名与用途：{{接口说明}}
- 方法与路径：{{METHOD /path}}
- 请求参数：{{参数表或代码定义}}
- 响应结构：{{响应结构或示例}}
- 错误码：{{错误码列表}}

## 输出
1. **一句话说明**：这个接口干什么，什么时候该用它（以及什么时候不该用）。
2. **请求**：参数表格（名称 | 类型 | 必填 | 默认 | 说明 | 约束），每个参数说明「写错了会怎样」。
3. **curl 示例**：一个最小请求 + 一个带可选参数的完整请求。
4. **响应**：成功响应示例 + 每个字段的含义；重点字段标注单位与格式（时间戳秒/毫秒、金额单位）。
5. **错误示例**：每个错误码一个真实响应体 + 用户该怎么处理。

**硬规则**：示例必须是合法可复制的；含糊参数（如「状态」）必须列出全部枚举值。`,
  },
  {
    sub: 'docs',
    slug: 'changelog-writer',
    title: '变更日志（Changelog）生成',
    summary: '把 git 提交记录整理成面向用户的变更日志。',
    tags: ['Changelog', '发布'],
    model: '通用',
    level: '通用',
    body: `把下方提交记录整理成变更日志。

## 提交记录
\`\`\`
{{git log 或 PR 列表}}
\`\`\`

## 版本信息
- 版本号：{{版本号}}
- 受众：{{终端用户 / 开发者 / 内部}}

## 输出
按 Keep a Changelog 规范：
1. **Added / Changed / Fixed / Removed / Deprecated / Security** 分组。
2. 每条从用户视角写（「修复了导入大文件时偶发的卡死」而不是「fix import bug」）。
3. **Breaking Changes** 单独置顶，写迁移方法。
4. 提交信息里的内部重构、CI 调整等用户无感的改动，合并成一行或省略。

**硬规则**：保留 issue/PR 编号；不确定影响的提交标注【需确认】而不是猜。`,
  },
  {
    sub: 'docs',
    slug: 'code-comment-guide',
    title: '注释与命名重构',
    summary: '按「注释解释为什么而非是什么」原则重写代码注释与命名。',
    tags: ['注释', '可读性'],
    model: '通用',
    level: '通用',
    body: `帮我改进下方代码的注释与命名。

\`\`\`{{语言}}
{{代码}}
\`\`\`

## 原则
- 注释只写「为什么」（业务原因、绕坑说明、不显然的约束），不复述代码。
- 命名要说清楚意图：变量名 = 它装什么，函数名 = 它做什么，布尔值 = is/has/can 前缀。
- 魔法数字必须提取为有名字的常量。

## 输出
1. **重构后代码**：只改注释与命名，不动逻辑。
2. **修改对照表**：原名 | 新名 | 理由（只列有改动的）。
3. **保留说明**：哪些现有注释是好的，为什么。

**硬规则**：不改任何逻辑行为；如果代码本身有明显 bug，指出但不要顺手修（避免混淆 diff）。`,
  },
  {
    sub: 'docs',
    slug: 'diagram-doc',
    title: '架构图与说明',
    summary: '用文本图 + 文字讲清系统结构',
    tags: ['文档', '架构图', '说明', '可视化'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位技术写作者。请用文本图（Mermaid / ASCII）配合说明讲清系统：
{{系统组件 + 关系}}

要求：
- 给出一个清晰的主流程图（请求如何流动）
- 用 Mermaid 语法绘制，节点命名贴合实际组件
- 配一段文字解释每个组件的职责与边界
- 标出同步 / 异步、外部依赖与故障传播
- 图后附「新人最常误解的 1 点」提示`,
  },
  {
    sub: 'docs',
    slug: 'readme-generator',
    title: 'README / API 文档生成',
    summary: '基于代码事实生成文档，结构规范，不含臆造的用法。',
    tags: ['文档', 'README', 'API'],
    model: '通用',
    level: '通用',
    body: `你是技术文档工程师。基于下面给出的**真实代码**生成文档。

## 代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 项目信息
- 名称：{{名称}}
- 一句话定位：{{一句话定位}}
- 目标读者：{{目标读者}}

## 铁律
**只写代码中确实存在的东西。** 以下行为一律禁止：
- 编造代码里没有的参数、配置项、返回值
- 写「例如你可以这样用」但示例无法通过类型检查
- 用占位内容填充（如 \`TODO\`、\`xxx\`）却不加标注
- 描述「未来会支持」的功能

如果发现代码本身缺少必要信息（如缺类型标注、缺默认值），在文档末尾的「待确认」章节列出，不要在正文中猜测。

## 输出结构
### README
1. 一句话简介（不超过 20 字）
2. 它解决什么问题（3 句话以内）
3. 快速开始：安装 → 最小可运行示例 → 预期输出
4. 核心概念（如有必要）
5. API 参考
6. 常见问题
7. 项目状态与限制

### API 参考格式
每个导出项：
\`\`\`
#### 名称(参数1: 类型, 参数2?: 类型 = 默认值): 返回类型
一句话说明。
**参数**
- \`参数1\` — 说明，取值范围，越界行为
**返回** — 说明，异常情况下的行为
**抛出** — 错误类型与触发条件
**示例** — 可直接运行的代码
\`\`\`

### 待确认清单
列出你无法从代码确定的点。`,
  },
  {
    sub: 'docs',
    slug: 'runbook-writer',
    title: '运维手册编写',
    summary: '把故障处理经验写成可执行的 runbook',
    tags: ['文档', 'runbook', '运维', 'SOP'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位 SRE。请把以下故障处理经验写成 runbook：
{{故障类型 + 已验证的处理步骤 + 责任人}}

要求：
- 结构：症状 → 影响评估 → 诊断命令 → 修复步骤 → 验证 → 回滚
- 每步给出可直接复制的命令或链接
- 标注决策点（什么情况下走回滚 / 升级）
- 用「如果…则…」覆盖常见分支
- 结尾留「事后复盘 TODO」占位，便于持续改进`,
  },
  {
    sub: 'docs',
    slug: 'tutorial-writer',
    title: '上手教程编写',
    summary: '为 SDK / 工具写一页式快速上手',
    tags: ['文档', '教程', '开发者', 'DX'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位开发者体验作者。请为一页式上手教程：
{{项目 / SDK + 目标读者水平}}

要求：
- 30 秒内能跑起来的最小示例优先
- 按「安装 → 配置 → 第一个成功输出 → 下一步」递进
- 每步明确预期结果，便于读者自检
- 把坑集中到「常见问题」小节，不打断主线
- 结尾给一个能立即产生价值的小任务`,
  },
  {
    sub: 'generate',
    slug: 'api-client-gen',
    title: 'API 客户端生成',
    summary: '根据接口定义生成调用端代码',
    tags: ['生成', 'API', '客户端', 'SDK'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位全栈工程师。请根据接口定义生成客户端：
{{OpenAPI / 接口文档 + 目标语言}}

要求：
- 生成类型安全的方法（请求 / 响应建模）
- 统一错误处理与超时 / 重试默认配置
- 处理鉴权头与常见 content-type
- 给出调用示例与一个最小错误处理模板
- 标注需要人肉确认的不确定字段`,
  },
  {
    sub: 'generate',
    slug: 'boilerplate-scaffold',
    title: '项目脚手架生成',
    summary: '生成新项目的初始骨架：目录结构、配置、入口、CI 一套齐。',
    tags: ['脚手架', '项目初始化'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `帮我搭 {{项目类型}} 项目脚手架。

## 要求
- 语言/框架：{{语言与框架}}
- 用途：{{项目要做什么}}
- 规模预期：{{玩具 / 原型 / 长期维护的生产项目}}
- 必须包含：{{测试 / Lint / CI / Docker / 其他要求}}

## 输出
1. **目录树**：每个目录/文件一句话说明职责。
2. **关键文件内容**：入口、配置、路由/模块注册的完整代码（不是省略号）。
3. **工程化配置**：lint 规则要点、测试框架接入、{{CI}} 最小配置。
4. **README 精简版**：安装、启动、跑测试三条命令。
5. **后续步骤**：建议的 first commit 之后的开发顺序。

**硬规则**：生产级项目必须含错误处理与日志骨架；所有配置给出「为什么选这个默认值」。`,
  },
  {
    sub: 'generate',
    slug: 'cli-tool-scaffold',
    title: '命令行工具脚手架生成',
    summary: '一句话描述生成带参数解析、帮助文档、错误码的 CLI 工具骨架。',
    tags: ['CLI', '脚手架', 'Python/Node'],
    model: '通用',
    level: '通用',
    body: `请为我生成一个命令行工具 \`{{工具名}}\` 的完整骨架。

## 工具用途
{{一句话用途}}

## 技术选型
语言：{{Python 3.11 / Node 20 / Go 1.22}}

## 必须包含
1. **参数解析**：子命令结构 \`{{工具名}} <command> [options]\`，支持 \`--help\`、\`--version\`、\`--verbose\`。
2. **配置优先级**：命令行参数 > 环境变量 > 配置文件 > 默认值，并在 help 中说明。
3. **错误码体系**：0 成功 / 1 通用错误 / 2 参数错误 / 3 网络错误 / 4 权限错误，每个错误输出结构化信息（含 \`code\`、\`message\`、\`hint\`）。
4. **日志**：verbose 模式输出 debug 日志，默认只输出 info 及以上，日志带时间戳与级别。
5. **优雅退出**：捕获 SIGINT，清理临时资源后退出。

## 输出
- 完整可运行代码（单文件优先）
- 一份 README 片段：安装方式 + 3 个使用示例 + 退出码说明
- 后续扩展点清单（我只想看到 3-5 条真正值得扩展的）`,
  },
  {
    sub: 'generate',
    slug: 'code-converter',
    title: '跨语言代码转换',
    summary: '把代码从一种语言翻译到另一种，保持语义并适配习惯用法。',
    tags: ['代码转换', '移植'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `把下方代码从 {{源语言}} 翻译到 {{目标语言}}。

\`\`\`{{源语言}}
{{待转换代码}}
\`\`\`

## 要求
1. **语义优先**：行为必须等价；类型系统差异（可空性、数值范围、错误处理模型）怎么映射要说明。
2. **习惯用法**：用目标语言的惯例重写（异常→Result/错误码、回调→async 等），不要逐行硬翻。
3. **依赖映射**：源语言的库调用映射到目标语言什么库？没有等价物的怎么替代？

## 输出
1. 转换后的完整代码。
2. **转换说明表**：不直译的点 | 源写法 | 目标写法 | 原因。
3. **风险点**：语义可能不一致的地方（整数溢出、浮点精度、时区、并发模型），建议怎么验证。

**硬规则**：目标语言没有的语义不许静默吞掉，必须显式处理或标注 TODO。`,
  },
  {
    sub: 'generate',
    slug: 'code-from-spec',
    title: '规格说明转代码',
    summary: '从需求规格直接生成带测试的可用代码，先澄清再动手。',
    tags: ['代码生成', '需求实现'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `根据规格实现代码。

## 规格
{{需求描述或接口规格}}

## 环境
- 语言：{{语言与版本}}
- 依赖限制：{{能用/不能用的库}}
- 运行环境：{{运行环境}}

## 工作流程
1. **澄清**：列出规格中模糊或缺失的点，给出你的默认假设（用户可纠正后再继续）。
2. **实现计划**：拆成几个函数/模块，各自职责一句话。
3. **代码**：完整可运行，关键决策处写「为什么」注释。
4. **测试**：正常路径 + 边界（空、极大、非法输入）+ 至少 1 个异常场景。
5. **自检**：对照规格逐条勾验收。

**硬规则**：不编造不存在的 API；依赖版本不确定时说明并给出验证方式。`,
  },
  {
    sub: 'generate',
    slug: 'feature-from-spec',
    title: '从需求描述生成完整功能代码',
    summary: '给出需求与技术栈，产出可直接落地的实现代码、边界处理与使用示例。',
    tags: ['代码生成', '需求落地', '全流程'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `你是一位资深 {{语言}} 工程师，严格遵循「最小可用 + 可读优先」的编码风格。

## 任务
根据下方需求，实现功能 {{功能名称}}。

## 需求描述
{{需求描述}}

## 技术栈
- 语言/版本：{{技术栈}}
- 已有依赖：{{已有依赖}}
- 运行环境：{{运行环境}}

## 实现约束
1. 不引入需求之外的抽象层、配置项或设计模式。
2. 每个函数不超过 40 行；超过则拆分，并说明拆分理由。
3. 必须处理以下边界：空输入、超长输入、非法类型、并发/重复调用。
4. 不使用任何已废弃 API；不确定时标注 \`// TODO: 需确认\` 而不是猜测。
5. 注释只写「为什么」，不写「做了什么」。

## 输出格式
1. **实现代码**：单个代码块，包含完整 import。
2. **关键决策**（3 条以内，每条一句话）：说明你为什么这么写。
3. **使用示例**：一段可直接运行的最小调用代码。
4. **风险与遗漏**：列出你判断需要我确认的点，没有就写「无」。

不要输出「好的，以下是代码」这类客套话，直接从代码开始。`,
  },
  {
    sub: 'generate',
    slug: 'mock-data-gen',
    title: '造测试数据',
    summary: '按 schema 生成逼真且多样的测试数据',
    tags: ['生成', '测试数据', 'fixture', 'mock'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位后端工程师。请生成测试数据集：
{{数据模型 / 字段约束 + 目标规模 + 用途}}

要求：
- 字段值符合约束（类型 / 范围 / 格式 / 关联外键）
- 覆盖典型、边界、异常三类样本
- 提供可复现的生成脚本或 SQL / JSON
- 若需敏感字段，用脱敏或虚构值
- 说明数据与生产分布的差异及注意事项`,
  },
  {
    sub: 'generate',
    slug: 'unit-test-from-code',
    title: '由代码生成单测',
    summary: '为给定函数补全单元测试',
    tags: ['生成', '单测', '测试', '代码'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位测试工程师。请为下面代码生成单元测试：
{{函数 / 类代码 + 语言与测试框架}}

要求：
- 覆盖正常路径、边界值、异常路径三类用例
- 使用表格驱动或参数化写法，避免重复
- 每个用例一行注释说明测的是什么
- Mock 外部依赖，不触发真实网络 / IO
- 输出可直接运行的测试文件，含必要 import`,
  },
  {
    sub: 'refactor',
    slug: 'circular-dep-fix',
    title: '循环依赖消除',
    summary: '找出并解开模块间的循环引用',
    tags: ['重构', '依赖', '循环引用', '解耦'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位架构师。请消除下面的循环依赖：
{{模块依赖关系 / 相关代码}}

要求：
- 画出依赖环并指出被破坏的层级
- 给出抽接口 / 下沉公共层 / 依赖注入等解法
- 选择改动最小且方向正确的那一种
- 提供重构后的依赖图
- 提醒测试覆盖这些边界，防回归`,
  },
  {
    sub: 'refactor',
    slug: 'design-pattern-advice',
    title: '设计模式适配建议',
    summary: '判断该不该用设计模式，用哪个，先看代码味道再开方。',
    tags: ['设计模式', '代码味道'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `帮我判断下方代码是否需要设计模式，需要哪个。

\`\`\`{{语言}}
{{代码与上下文说明}}
\`\`\`

## 我的困扰
{{switch 越来越长 / 到处 new 同一类对象 / 改一个需求要动五个文件 / 其他}}

## 输出
### 1. 代码味道诊断
属于哪类味道（长参数列表、散弹式修改、依恋情结、条件复杂度……），证据是什么。

### 2. 该不该上模式
先回答这个问题：**如果这段代码一年内不会改，还需要模式吗？** 只有变化方向真实存在才值得抽象。

### 3. 处方
推荐的 1 个模式（最多 2 个备选）：
- 重构后的目标结构（示意代码）
- 引入的成本（类数量、理解门槛）
- 不推荐的常见误用（大家怎么把这个模式用错）

### 4. 不用模式的替代解
有没有更轻的办法（提取函数、查表法、配置化）？

**硬规则**：禁止罗列 23 种模式科普；结论必须绑定到这段代码的具体变化点。`,
  },
  {
    sub: 'refactor',
    slug: 'extract-function',
    title: '函数抽取重构',
    summary: '把长函数按职责切块抽取，保持行为不变的小步重构。',
    tags: ['重构', '函数拆分'],
    model: '通用',
    level: '通用',
    body: `对下方长函数做「函数抽取」重构。

\`\`\`{{语言}}
{{长函数代码}}
\`\`\`

## 原则
- 每次抽取一个内聚块，函数名 = 它做的事（动词开头）。
- 抽取后主函数应该读起来像目录/大纲。
- 中间变量变成参数或返回值时，命名要暴露数据流向。

## 输出
1. **切块方案**：先标出代码里的逻辑块及边界（注释形式），说明每块的职责。
2. **重构后代码**：完整、可直接替换原函数。
3. **重构对照**：新函数名 | 职责 | 从原函数第几行到第几行。
4. **行为不变性检查**：哪些点容易在抽取时悄悄改变行为（副作用顺序、提前 return、异常传播），逐条确认没变。

**硬规则**：纯重构，不修 bug 不改算法；发现原代码问题列在最后「顺带发现」一节，不动手。`,
  },
  {
    sub: 'refactor',
    slug: 'god-object-split',
    title: '大对象拆分',
    summary: '把上帝类按职责拆成小模块',
    tags: ['重构', '拆分', '单一职责', '整洁'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位重构专家。请拆分下面的上帝类 / 大文件：
{{代码 + 当前痛点}}

要求：
- 按职责识别出隐含的子模块与边界
- 给出新的文件 / 类划分与依赖方向
- 提出渐进式迁移步骤，避免一次性大改
- 指出哪些方法其实该下沉到领域层
- 提供迁移后的目录结构与调用示例`,
  },
  {
    sub: 'refactor',
    slug: 'legacy-code-safe-refactor',
    title: '遗留代码安全重构',
    summary: '无测试覆盖的老代码：先 characterization 测试再重构。',
    tags: ['遗留代码', '测试'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `我要重构下方遗留代码，但它没有测试。

\`\`\`{{语言}}
{{遗留代码}}
\`\`\`

## 现状
- 改动动机：{{为什么要动它}}
- 风险等级：{{核心支付链路 / 一般业务 / 边缘功能}}
- 调用方数量：{{调用方情况}}

## 输出（按顺序）
1. **行为快照**：先列出这段代码当前所有可观察行为（含看起来像 bug 的行为——它们可能是有人依赖的）。
2. **characterization 测试**：为每个行为写「锁死现状」的测试用例（输入→断言当前输出，不管对错）。
3. **重构方案**：在测试保护下的小步重构序列，每步一个可独立提交的改动。
4. **观察区**：哪些行为是疑似 bug，重构完成后需要和 {{相关方}} 确认是否修复。

**硬规则**：重构期间禁止「顺手修 bug」——先锁行为再谈改变；每一步重构完测试必须全绿。`,
  },
  {
    sub: 'refactor',
    slug: 'performance-hunt',
    title: '性能瓶颈定位与优化',
    summary: '分析代码的时间/空间复杂度、IO 与锁竞争问题，给出按收益排序的优化方案。',
    tags: ['性能', '优化', '复杂度'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是性能优化工程师。下面是需要优化的代码与背景。

## 代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 性能现状
- 当前耗时/吞吐：{{量化指标}}
- 目标：{{量化目标}}
- 数据规模：{{数据量级}}
- 运行环境：{{CPU/内存/网络条件}}

## 分析要求
1. 先定位瓶颈，不要直接给优化代码。用以下分类逐个排查：
   - 算法复杂度（时间 O、空间 O）
   - 重复计算 / 缓存缺失
   - IO 次数（数据库 N+1、频繁小文件读写、未批量）
   - 锁竞争与串行等待
   - 内存分配（频繁创建临时对象、大对象拷贝）
   - 序列化开销
2. 每一条必须给出**量化估算**：优化前多少次操作 → 优化后多少次。
3. 按「收益 / 改动成本」从高到低排序，只推荐前 3 项。
4. 明确指出哪些优化**不值得做**（收益 < 5% 或引入复杂度过高）。

## 输出格式
### 瓶颈定位
（表格：位置 | 类型 | 当前开销 | 量化说明）

### 优化方案（按优先级）
每项包含：改动前代码 → 改动后代码 → 预期收益 → 副作用

### 不推荐项
（列出你评估后认为不该做的优化及理由）

### 验证方法
（如何测量优化是否生效：指标、工具、基准代码片段）`,
  },
  {
    sub: 'refactor',
    slug: 'refactor-with-diff',
    title: '安全重构助手（带改动说明）',
    summary: '在不改变外部行为的前提下重构代码，逐条列出改动点、收益与风险。',
    tags: ['重构', '代码质量', '最佳实践'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是代码重构专家，信条是「先证明行为不变，再谈优雅」。

## 待重构代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 上下文
- 这段代码被 {{调用方}} 调用
- 可接受的改动范围：{{仅内部实现 / 可调整签名}}
- 团队代码规范：{{规范要点，没有则填「无特殊规范」}}

## 重构要求
1. **行为等价**是硬约束。若某处改动可能改变行为，必须单独标出并说明。
2. 按收益从高到低排序做以下事情：
   - 消除重复（三处以上重复才提取，两处不动）
   - 降低圈复杂度（拆分条件嵌套、提前返回）
   - 命名修正（变量名要能体现业务含义，不要 \`data\`/\`tmp\`/\`res\`）
   - 拆分过长函数
3. **不要**做这些：加设计模式、加泛型抽象、加配置项、加日志、改错误处理风格。

## 输出格式
### 重构后代码
（完整代码，不要省略）

### 改动清单
| # | 改动点 | 类型 | 收益 | 风险 |
|---|--------|------|------|------|
| 1 | ... | 命名/去重/拆分/简化 | ... | 无 / 说明 |

### 我建议但不建议现在做的
（列出你看到但认为不该在当前改动中处理的问题，附理由）`,
  },
  {
    sub: 'refactor',
    slug: 'strangler-fig',
    title: '绞杀者重构',
    summary: '在不停机前提下逐步替换老模块',
    tags: ['重构', '绞杀者模式', '迁移', '风险'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位资深工程师。请用绞杀者模式设计老模块替换：
{{遗留模块 + 新目标实现 + 约束}}

要求：
- 设计路由 / 适配层，让新旧并存
- 给出按流量或功能逐步切换的迁移批次
- 明确每步的可回滚点
- 标注数据双写 / 校验一致性策略
- 给出「何时彻底下线老代码」的判定标准`,
  },
  {
    sub: 'regex',
    slug: 'date-format-regex',
    title: '日期格式解析',
    summary: '写匹配多种日期写法的正则',
    tags: ['正则', '日期', '格式', '解析'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位数据工程师。请写正则识别并归一化日期：
{{支持的格式：ISO / 中文 / 带时区 等}}

要求：
- 给出可捕获年 / 月 / 日分组的正则
- 处理分隔符差异（- / / 年月日）
- 标注二义性（如 03/04/05 的解析歧义）
- 提供转为标准格式（ISO）的替换示例
- 提醒优先用语言内置日期库而非纯正则`,
  },
  {
    sub: 'regex',
    slug: 'email-phone-regex',
    title: '邮箱手机号正则',
    summary: '写兼顾正确与可读的联系方式匹配',
    tags: ['正则', '邮箱', '手机号', '校验'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '入门',
    body: `你是一位后端工程师。请写正则匹配 {{邮箱 / 手机号（含国家码）}}：
{{输入样本 + 允许的格式范围}}

要求：
- 给出正则与逐段注释（分组含义）
- 说明边界：是否允许空格、连字符、大小写
- 提示正则的局限（如邮箱规范的完备性）
- 提供校验函数片段（含 trim 与归一化）
- 给出常见误写（过度宽松 / 灾难性回溯）警告`,
  },
  {
    sub: 'regex',
    slug: 'html-tag-regex',
    title: 'HTML 标签提取',
    summary: '用正则安全地抽取标签内容',
    tags: ['正则', 'HTML', '解析', '提取'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位爬虫工程师。请用正则从 HTML 抽取 {{目标}}：
{{HTML 片段样例}}

要求：
- 给出匹配正则并逐段注释
- 说明嵌套标签 / 属性顺序带来的坑
- 提醒「用正则解析 HTML 的边界」，必要时改用解析器
- 提供提取后的清洗步骤（去空白 / 解码实体）
- 给出防贪婪匹配错误的写法`,
  },
  {
    sub: 'regex',
    slug: 'log-parse-regex',
    title: '日志解析正则',
    summary: '为日志格式写结构化解析正则，含字段提取与容错。',
    tags: ['正则', '日志', '解析'],
    model: '通用',
    level: '进阶',
    body: `为下方日志格式写解析正则。

## 日志样例（3-5 行）
\`\`\`
{{多行日志样例}}
\`\`\`

## 目标
提取字段：{{想提取的字段，如时间/级别/模块/traceId/消息}}，语言：{{语言}}

## 输出
1. **格式分析**：先归纳这套日志的格式规律（定界符、可选字段顺序、异常堆栈怎么多行续接）。
2. **正则**：命名分组版 + 逐组说明。
3. **解析代码**：目标语言里把每行转成 dict/对象的完整片段，处理：
   - 匹配失败的行怎么处理（跳过并计数？进 dead letter？）
   - 多行堆栈怎么归并到上一条
4. **容错测试**：构造 3 条畸形日志（缺字段、空消息、超长行）验证不崩。

**硬规则**：宁可窄匹配也不要让一行脏日志拖垮整个解析任务。`,
  },
  {
    sub: 'regex',
    slug: 'regex-builder',
    title: '正则表达式构造与讲解',
    summary: '给出可用正则、逐段讲解、测试用例与性能陷阱提示。',
    tags: ['正则', '文本处理', '工具'],
    model: '通用',
    level: '通用',
    body: `你是正则表达式专家。

## 匹配需求
- 目标文本样例：
\`\`\`
{{样例文本}}
\`\`\`
- 需要匹配/提取：{{目标}}
- 需要排除：{{反例}}
- 正则引擎：{{PCRE / Python re / JavaScript / Go RE2 / POSIX}}

## 输出
### 1. 推荐正则
\`\`\`
{{正则}}
\`\`\`

### 2. 逐段讲解
| 片段 | 含义 | 为什么这么写 |

### 3. 测试集
表格：测试用例 | 是否应匹配 | 实际结果 | 捕获组内容
至少包含：4 个正例、3 个反例、2 个边界情况（空串、超长、特殊字符）

### 4. 引擎差异提示
说明该正则在目标引擎下的注意点（如 JS 不支持后行断言的变长、Go RE2 不支持回溯引用）。

### 5. 性能提示
- 是否存在灾难性回溯（嵌套量词 \`(a+)+\`）？
- 是否有更简单的等价写法？
- 如果用于高频调用，建议的替代方案（如字符串方法、解析器）。

### 6. 代码示例
给出目标语言的使用代码（含编译/预编译、命名捕获组、错误处理）。`,
  },
  {
    sub: 'regex',
    slug: 'regex-explainer',
    title: '正则表达式解读',
    summary: '把看不懂的正则翻译成结构图和大白话。',
    tags: ['正则', '逆向'],
    model: '通用',
    level: '通用',
    body: `帮我解读这个正则：

\`\`\`
{{正则表达式}}
\`\`\`

（用途线索：{{在什么代码/场景里见到的，可选}}）

## 输出
1. **一句话功能**：这个正则整体在干什么。
2. **结构拆解**：按语法单元拆成树/表格：片段 | 含义 | 捕获组编号或名字。
3. **执行过程**：拿一个典型输入 {{样例输入}}，走一遍匹配过程（哪个字符在哪一段被吃掉）。
4. **边界行为**：空串能过吗？贪婪匹配会吃多少？
5. **潜在问题**：误伤/漏配风险、性能隐患，以及更清晰的替代写法（如有）。

**硬规则**：绝不猜。如果方言不明确会改变语义（如 \\d 是否含全角），明确指出并两种都解释。`,
  },
  {
    sub: 'regex',
    slug: 'regex-migration',
    title: '正则跨方言迁移',
    summary: '正则从一种语言迁到另一种的兼容性检查与改写。',
    tags: ['正则', '迁移'],
    model: '通用',
    level: '通用',
    body: `把这个正则从 {{源方言}} 迁移到 {{目标方言}}：

\`\`\`
{{原正则}}
\`\`\`

## 输出
1. **兼容性检查表**：逐特性检查——命名分组语法、前后查找支持、\\b 行为、Unicode 类（\\p{...}）、s/m/x 标志、 possessive 量词，源目标是否一致。
2. **改写后的正则**：目标方言原生写法（不是硬复制）。
3. **行为差异警告**：哪些输入在两边结果不同（如 Unicode、换行处理）。
4. **验证用例**：5 组输入在两方言下的预期结果对照。

**硬规则**：目标方言不支持的特性必须给替代实现（如用回调替代 lookbehind），不能静默删除。`,
  },
  {
    sub: 'regex',
    slug: 'regex-writer',
    title: '正则表达式生成',
    summary: '从匹配样例反推正则，输出带逐段解释和测试用例的表达式。',
    tags: ['正则', '文本处理'],
    model: '通用',
    level: '通用',
    body: `帮我写正则表达式。

## 需求
- 匹配目标：{{想匹配什么}}
- 必须匹配的样例：{{应命中的字符串}}
- 必须不匹配的样例：{{不应命中的字符串}}
- 方言：{{JavaScript / Python / PCRE / grep}}

## 输出
1. **正则**：优先给可读的（带命名分组/注释），再给紧凑版。
2. **逐段解释**：每个元字符/分组在干什么，用表格。
3. **测试矩阵**：对上面每个样例标注命中/不命中 + 捕获组内容。
4. **性能与坑**：有没有回溯风险？贪婪还是懒惰更合适？多行/大小写标志要不要？

**硬规则**：如果有多个合理粒度（宽匹配 vs 严匹配），两个都给并说明取舍；不要只给一个「万能但不精确」的。`,
  },
  {
    sub: 'review',
    slug: 'accessibility-audit',
    title: '无障碍评审',
    summary: '按 WCAG 检查前端可访问性',
    tags: ['评审', 'a11y', 'WCAG', '前端'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位无障碍专家。请评审下面前端代码的 a11y 问题：
{{组件 / 页面代码}}

要求：
- 检查语义标签、alt、label、焦点顺序、对比度
- 按 WCAG 2.1 AA 标注不达标项
- 每条给出具体修复代码或属性
- 指出屏幕阅读器下的预期体验缺口
- 提供一份可勾选的修复清单`,
  },
  {
    sub: 'review',
    slug: 'code-review-checklist',
    title: '结构化代码审查',
    summary: '按严重级别分级输出审查意见，区分阻断问题、建议与吹毛求疵。',
    tags: ['Code Review', '代码质量', '协作'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `你是团队里最严格但最讲道理的 Code Reviewer。

## 待审查变更
\`\`\`diff
{{diff 或代码}}
\`\`\`

## 变更意图
{{这个 PR 想解决什么问题}}

## 审查维度
按以下顺序检查，逐项给出结论（通过 / 有问题）：
1. **正确性**：逻辑是否符合意图？边界是否覆盖？并发是否安全？
2. **安全性**：注入、越权、敏感信息泄露、依赖漏洞。
3. **性能**：是否有 N+1、无界循环、内存泄漏、不必要的同步阻塞。
4. **可维护性**：命名、函数长度、重复代码、隐含耦合。
5. **可观测性**：错误是否有上下文？关键路径是否有日志/指标？
6. **测试**：改动是否覆盖了测试？测试是否真的能失败？

## 输出格式
按严重级别分组，每条格式：
\`[文件路径:行号] 问题 → 为什么是问题 → 具体改法\`

- 🔴 **阻断**：必须改，否则会出线上问题
- 🟡 **建议**：强烈建议改，不改要给出理由
- 🟢 **可选**：风格类、可读性类，作者可自行决定

**硬规则**：
- 不提「建议加注释」这种无信息量的意见。
- 每条问题必须给具体改法，不能只说「这里有问题」。
- 如果整体没问题，直接说「LGTM」并说明你检查了什么，不要凑数。`,
  },
  {
    sub: 'review',
    slug: 'db-query-review',
    title: 'SQL 查询评审',
    summary: '在 CR 中审查慢查询与反模式',
    tags: ['评审', 'SQL', '性能', 'CR'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位数据工程师。请评审下面 SQL 的健壮性与性能：
{{SQL + 表结构要点}}

要求：
- 找出 SELECT *、隐式转换、缺失索引、深分页等反模式
- 指出注入风险与参数化缺失
- 对慢点给出改写（JOIN 顺序 / 子查询展开 / 覆盖索引）
- 提醒大表上的锁与事务时长
- 提供改写版与验证方式（EXPLAIN）`,
  },
  {
    sub: 'review',
    slug: 'performance-review',
    title: '性能代码评审',
    summary: '在 CR 中揪出性能隐患',
    tags: ['评审', '性能', 'CR', '优化'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位性能方向 reviewer。请评审下面代码的性能问题：
{{代码 + 场景（规模 / QPS）}}

要求：
- 找出 N+1、不必要拷贝、同步阻塞、频繁分配等隐患
- 对每条给出影响量级与修复方向
- 区分「现在就改」与「记入 TODO」的优先级
- 提供改写前后的关键片段对比
- 提醒加上基准测试，避免凭感觉优化`,
  },
  {
    sub: 'review',
    slug: 'pr-nitpicker',
    title: '代码风格细抠（nit 级）',
    summary: '不谈架构只抠细节：命名、边界、一致性、坏味道。',
    tags: ['代码风格', '细节'],
    model: '通用',
    level: '通用',
    body: `对下方代码做「细节级」审查——不谈架构，只抠手艺。

\`\`\`{{语言}}
{{代码}}
\`\`\`

## 检查清单
1. **命名**：名字和它实际干的事一致吗？同一概念在不同地方名字统一吗？
2. **边界**：off-by-one、区间开闭、空集合、null/undefined 处理。
3. **一致性**：错误处理风格、返回值约定、缩进与格式，全文统一吗？
4. **坏味道**：重复代码（3 处以上才值得抽象，2 处先忍）、过深嵌套、布尔参数、注释掉的死代码。
5. **偷偷的过度设计**：只有一个实现的接口、永远为 false 的分支、没人抛的异常捕获。

## 输出
按行号列出，每条：\`行号 | 问题 | 改法\`。改法直接给代码片段。

**硬规则**：所有意见都是 [nit] 级（不阻塞合并），但每条都要具体可执行；禁止空泛的「建议优化」。`,
  },
  {
    sub: 'review',
    slug: 'pr-review-checklist',
    title: 'PR 代码审查',
    summary: '按正确性、安全、性能、可维护性四维审查 PR diff。',
    tags: ['代码审查', 'PR'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `你是严格的代码审查者。审查下方 PR。

## 变更内容
\`\`\`diff
{{diff 或代码}}
\`\`\`

## 背景
- 变更目的：{{这个 PR 要解决什么}}
- 影响范围：{{影响的用户/模块}}

## 审查维度
1. **正确性**：边界条件、错误路径、并发、空值。找出「只在 happy path 上对」的代码。
2. **安全**：注入、越权、密钥泄漏、不安全的依赖。
3. **性能**：循环里的重复计算、N+1、无界内存、不必要的同步。
4. **可维护性**：命名、函数长度、抽象层次、测试覆盖了新行为吗。

## 输出格式
每条评论：\`\`\`[严重度 P0/P1/P2] 位置 | 问题 | 为什么是问题 | 建议改法（可给代码）\`\`\`

结尾给：**合并结论**（可合 / 修改后可合 / 需重做）+ 三个做得好的点（好的实践值得说出口）。

**硬规则**：区分「必须改」和「个人偏好」；偏好类评论加 [nit] 前缀。`,
  },
  {
    sub: 'review',
    slug: 'security-code-audit',
    title: '安全代码审计',
    summary: '按 OWAP Top10 视角扫描代码，输出可利用性评估。',
    tags: ['安全', '审计'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `对下方代码做安全审计。

\`\`\`{{语言}}
{{代码}}
\`\`\`

## 上下文
- 暴露面：{{对外接口 / 内部服务 / 定时任务}}
- 数据敏感度：{{涉及的数据：用户 PII / 支付 / 普通业务}}

## 输出
### 1. 漏洞清单
按 OWASP 类别扫描（注入、失效的访问控制、加密失败、SSRF、反序列化、XXE、CSRF、日志泄漏敏感信息……与该代码相关的都查）：

漏洞 | 位置 | 攻击场景（具体到攻击者怎么利用） | 严重度（CVSS 思路） | 修复代码

### 2. 信任边界标注
哪些输入来自不可信方，传播路径上哪里做了校验，哪里裸奔。

### 3. 加固建议
即使没有直接漏洞，也给出纵深防御建议（最小权限、参数化查询、输出编码、速率限制）。

**硬规则**：每个漏洞必须有具体攻击场景，禁止「可能存在风险」这种废话；修复代码要能直接用。`,
  },
  {
    sub: 'review',
    slug: 'test-coverage-review',
    title: '测试质量审查',
    summary: '审查测试本身：测对了吗，还是只是刷覆盖率。',
    tags: ['测试', '覆盖率'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `审查下方测试代码的质量。

## 被测代码
\`\`\`
{{被测函数/模块}}
\`\`\`

## 测试代码
\`\`\`
{{测试代码}}
\`\`\`

## 输出
### 1. 覆盖缺口矩阵
场景 | 已测? | 重要性
至少检查：正常路径、边界值、非法输入、空集合、并发、网络/IO 失败、超时。

### 2. 测试反模式扫描
- 断言过弱（只断言不抛异常 / 只断言 truthy）
- 测了 mock 而不是被测逻辑（mock 返回值直接被断言）
- 测试间共享可变状态、执行顺序依赖
- 一个测试塞多个场景（失败时不知道哪个断言坏了）
- 脆弱断言（断言完整快照，任何重构都红）

### 3. 补测建议
按风险排序给出缺失的测试用例，附可直接用的测试代码骨架。

**硬规则**：区分「覆盖率数字覆盖了」和「行为真的被验证了」；给出能抓到真实 bug 的测试，不是凑数测试。`,
  },
  {
    sub: 'testing',
    slug: 'bug-repro-test',
    title: 'Bug 复现测试',
    summary: '把 bug 报告转成最小可复现测试，先红后绿。',
    tags: ['复现', '回归测试'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `把这个 bug 转成测试用例。

## Bug 报告
- 现象：{{期望 vs 实际}}
- 复现步骤：{{复现步骤}}
- 环境：{{环境信息}}
- 相关代码：
\`\`\`
{{疑似相关代码}}
\`\`\`

## 输出
1. **最小化**：剥离复现步骤里不必要的条件，找出触发 bug 的最小输入（说明你排除了什么）。
2. **失败的测试**：先写一个当前会失败（红）的测试，精确断言期望行为。
3. **触发条件分析**：这个 bug 的触发窗口是什么（特定输入/状态/时序），有没有相邻的未触发变体也该测。
4. **回归防线**：修复后，这个测试之外还应加什么测试防止同类问题（同族 bug 检查）。
5. **修复建议**：如果你能定位根因，给出修复方向（但测试不依赖你的修复假设）。

**硬规则**：测试必须稳定复现（偶发 bug 先想办法去随机化）；断言的是「期望行为」而非「当前行为」。`,
  },
  {
    sub: 'testing',
    slug: 'contract-test',
    title: '契约测试',
    summary: '为服务间接口写契约测试',
    tags: ['测试', '契约', '微服务', '集成'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位测试架构师。请为服务间接口设计契约测试：
{{提供方 / 消费方 + 接口契约}}

要求：
- 用消费者驱动契约（CDC）思路列出关键场景
- 给出请求 / 响应断言的最小必要集合
- 说明如何在 CI 中独立验证双方
- 标注破坏性变更如何被契约捕获
- 提供一份示例契约（如 Pact 风格）骨架`,
  },
  {
    sub: 'testing',
    slug: 'e2e-test-plan',
    title: '端到端测试用例设计',
    summary: '从用户旅程推导 E2E 场景，输出用例集与自动化优先级。',
    tags: ['E2E', '测试设计'],
    model: 'Claude / GPT-4o',
    level: '进阶',
    body: `为下方功能设计端到端测试。

## 被测功能
{{功能描述}}

## 用户旅程
{{典型用户怎么一步步用这个功能}}

## 关键约束
- 失败成本：{{出了问题影响多大}}
- 自动化预算：{{能维护几条 E2E}}

## 输出
### 1. 旅程拆解
把用户旅程拆成可独立验证的步骤，标出每步的前置条件和成功标准。

### 2. 用例集
用例名（用户视角描述）| 步骤 | 断言点 | 优先级。
覆盖：主路径、关键分支、中断恢复（断网/刷新后继续）、数据状态前置（新用户/老用户）。

### 3. 取舍
E2E 贵——哪些用例降级到集成测试/单测更划算？给出分层建议。

### 4. 稳定性设计
选择器策略（避免脆弱选择器）、等待策略、测试数据隔离、失败重试边界。

**硬规则**：E2E 只测「跨系统协作才能验证」的东西；纯逻辑不许进 E2E。`,
  },
  {
    sub: 'testing',
    slug: 'load-test-plan',
    title: '压测方案',
    summary: '设计可落地的负载与容量测试',
    tags: ['测试', '压测', '容量', '性能'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位性能工程师。请设计压测方案：
{{系统 + 目标 SLA + 预期峰值}}

要求：
- 定义压测目标（吞吐 / 延迟分位 / 拐点）
- 设计场景（基线 / 峰值 / 混合 / 破坏性）
- 给出工具选型（k6 / JMeter / 云压测）与脚本要点
- 说明监控指标与「停止压测」的红线
- 提供结果解读框架（瓶颈定位 / 容量结论）`,
  },
  {
    sub: 'testing',
    slug: 'mutation-testing',
    title: '变异测试',
    summary: '用变异测试评估用例有效性',
    tags: ['测试', '变异测试', '覆盖', '质量'],
    model: '代码模型（Claude / Cursor / GPT）',
    level: '进阶',
    body: `你是一位测试质量专家。请说明并落地变异测试：
{{代码库语言 + 现有测试}}

要求：
- 解释变异得分（mutation score）为何比行覆盖更可信
- 推荐工具（如 Stryker / mutmut）并给配置要点
- 设计先从核心模块切入的范围
- 解读「存活变异」意味着用例漏测什么
- 给出把变异测试接进 CI 的渐进方案`,
  },
  {
    sub: 'testing',
    slug: 'test-case-design',
    title: '测试用例设计与生成',
    summary: '按等价类、边界值、错误推测法设计用例，覆盖正常/边界/异常三条线。',
    tags: ['测试', '单元测试', '覆盖率'],
    model: '通用',
    level: '通用',
    body: `你是测试工程师。为下面的函数设计并执行测试用例。

## 被测代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 测试框架
{{pytest / Jest / Go testing / JUnit}}

## 设计方法（必须用上）
1. **等价类划分**：把输入分成若干类，每类取一个代表值。
2. **边界值分析**：对每个数值/长度边界，取 min-1、min、min+1、正常、max-1、max、max+1。
3. **错误推测**：基于经验猜测容易出错的地方（空值、特殊字符、时区、浮点精度、编码）。
4. **状态转换**：若涉及状态机，覆盖所有合法转换 + 至少 2 条非法转换。

## 输出
### 用例清单（表格）
| 编号 | 场景 | 输入 | 期望输出 | 设计方法 | 优先级 |
（优先级：P0 阻断 / P1 核心 / P2 边缘）

### 测试代码
完整可运行，包含：
- 正常路径（至少覆盖主要分支）
- 边界与异常路径
- 每个断言写清「期望什么」而不是 \`assert result is not None\` 这种无效断言

### 覆盖缺口
说明哪些代码路径**无法**被单测覆盖（如需 Mock 外部服务、涉及真实 IO），以及建议的替代验证方式。`,
  },
  {
    sub: 'testing',
    slug: 'test-data-factory',
    title: '测试数据工厂',
    summary: '生成可组合、可控的测试数据构造器，告别手写 fixture。',
    tags: ['测试数据', 'Fixture'],
    model: '通用',
    level: '通用',
    body: `为下方数据结构设计测试数据工厂。

\`\`\`
{{数据结构/schema/类型定义}}
\`\`\`

## 用途
- 场景：{{单测 / 集成测试 / E2E 预置}}
- 语言：{{语言}}
- 需要的变体：{{合法最小、完整、非法字段、边界值}}

## 输出
1. **工厂代码**：
   - 默认生成「合法且无趣」的数据（所有字段有效但无特殊含义）
   - 参数覆盖式：\`makeUser({ email: 'x@y.z' })\` 只改指定字段
   - 预制变体：\`makeUser.banned()\`、\`makeUser.noEmail()\` 等语义化快捷方式
   - 关联数据级联生成（order → user 自动建）
2. **变体清单**：结合业务列出你建议的语义化变体及用途。
3. **反模式警告**：工厂与真实 schema 漂移怎么办（schema 校验进工厂测试）。

**硬规则**：工厂产的数据必须通过 schema 校验（非法变体除外）；禁止随机字段——随机让失败不可复现。`,
  },
  {
    sub: 'testing',
    slug: 'unit-test-generator',
    title: '单元测试生成',
    summary: '为函数生成分层单测：等价类、边界值、异常路径全覆盖。',
    tags: ['单元测试', '测试生成'],
    model: 'Claude / GPT-4o',
    level: '通用',
    body: `为下方代码生成单元测试。

\`\`\`{{语言}}
{{被测代码}}
\`\`\`

## 环境
- 测试框架：{{Jest / pytest / JUnit / Vitest}}
- Mock 策略：{{外部依赖怎么处理}}

## 输出
1. **测试设计先行**：先列表——用例名 | 输入类别（等价类/边界/异常）| 预期行为，说明为什么选这些用例。
2. **测试代码**：每个用例独立可读（AAA 结构：Arrange-Act-Assert），用例名用完整句子描述行为。
3. **边界专项**：空输入、单元素、极大值、非法类型至少各一个。
4. **Mock 说明**：哪些依赖被 mock、mock 的行为设计是否贴近真实。

**硬规则**：测试失败时的报错必须能定位问题；禁止快照测试刷覆盖；不测私有实现细节，只测公开行为。`,
  },
];
