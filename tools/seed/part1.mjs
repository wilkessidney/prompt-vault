/* 代码开发 / 提示词工程 / 数据分析 */
export default [
  // ============ 代码开发 ============
  {
    cat: 'coding', sub: 'generate', slug: 'feature-from-spec',
    title: '从需求描述生成完整功能代码',
    summary: '给出需求与技术栈，产出可直接落地的实现代码、边界处理与使用示例。',
    tags: ['代码生成', '需求落地', '全流程'],
    model: 'Claude / GPT-4o', level: '通用',
    content: `你是一位资深 {{语言}} 工程师，严格遵循「最小可用 + 可读优先」的编码风格。

## 任务
根据下方需求，实现功能 {{功能名称}}。

## 需求描述
{{需求描述}}

## 技术栈
- 语言/版本：{{技术栈}}
- 已有依赖：{{已有依赖}}
- 运行环境：{{运行环境}}

## 实现约束
1. 不引入需求之外的抽象层、配置项或设计模式。
2. 每个函数不超过 40 行；超过则拆分，并说明拆分理由。
3. 必须处理以下边界：空输入、超长输入、非法类型、并发/重复调用。
4. 不使用任何已废弃 API；不确定时标注 \`// TODO: 需确认\` 而不是猜测。
5. 注释只写「为什么」，不写「做了什么」。

## 输出格式
1. **实现代码**：单个代码块，包含完整 import。
2. **关键决策**（3 条以内，每条一句话）：说明你为什么这么写。
3. **使用示例**：一段可直接运行的最小调用代码。
4. **风险与遗漏**：列出你判断需要我确认的点，没有就写「无」。

不要输出「好的，以下是代码」这类客套话，直接从代码开始。`,
  },
  {
    cat: 'coding', sub: 'generate', slug: 'cli-tool-scaffold',
    title: '命令行工具脚手架生成',
    summary: '一句话描述生成带参数解析、帮助文档、错误码的 CLI 工具骨架。',
    tags: ['CLI', '脚手架', 'Python/Node'],
    model: '通用', level: '通用',
    content: `请为我生成一个命令行工具 \`{{工具名}}\` 的完整骨架。

## 工具用途
{{一句话用途}}

## 技术选型
语言：{{Python 3.11 / Node 20 / Go 1.22}}

## 必须包含
1. **参数解析**：子命令结构 \`{{工具名}} <command> [options]\`，支持 \`--help\`、\`--version\`、\`--verbose\`。
2. **配置优先级**：命令行参数 > 环境变量 > 配置文件 > 默认值，并在 help 中说明。
3. **错误码体系**：0 成功 / 1 通用错误 / 2 参数错误 / 3 网络错误 / 4 权限错误，每个错误输出结构化信息（含 \`code\`、\`message\`、\`hint\`）。
4. **日志**：verbose 模式输出 debug 日志，默认只输出 info 及以上，日志带时间戳与级别。
5. **优雅退出**：捕获 SIGINT，清理临时资源后退出。

## 输出
- 完整可运行代码（单文件优先）
- 一份 README 片段：安装方式 + 3 个使用示例 + 退出码说明
- 后续扩展点清单（我只想看到 3-5 条真正值得扩展的）`,
  },
  {
    cat: 'coding', sub: 'refactor', slug: 'refactor-with-diff',
    title: '安全重构助手（带改动说明）',
    summary: '在不改变外部行为的前提下重构代码，逐条列出改动点、收益与风险。',
    tags: ['重构', '代码质量', '最佳实践'],
    model: 'Claude / GPT-4o', level: '进阶',
    content: `你是代码重构专家，信条是「先证明行为不变，再谈优雅」。

## 待重构代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 上下文
- 这段代码被 {{调用方}} 调用
- 可接受的改动范围：{{仅内部实现 / 可调整签名}}
- 团队代码规范：{{规范要点，没有则填「无特殊规范」}}

## 重构要求
1. **行为等价**是硬约束。若某处改动可能改变行为，必须单独标出并说明。
2. 按收益从高到低排序做以下事情：
   - 消除重复（三处以上重复才提取，两处不动）
   - 降低圈复杂度（拆分条件嵌套、提前返回）
   - 命名修正（变量名要能体现业务含义，不要 \`data\`/\`tmp\`/\`res\`）
   - 拆分过长函数
3. **不要**做这些：加设计模式、加泛型抽象、加配置项、加日志、改错误处理风格。

## 输出格式
### 重构后代码
（完整代码，不要省略）

### 改动清单
| # | 改动点 | 类型 | 收益 | 风险 |
|---|--------|------|------|------|
| 1 | ... | 命名/去重/拆分/简化 | ... | 无 / 说明 |

### 我建议但不建议现在做的
（列出你看到但认为不该在当前改动中处理的问题，附理由）`,
  },
  {
    cat: 'coding', sub: 'refactor', slug: 'performance-hunt',
    title: '性能瓶颈定位与优化',
    summary: '分析代码的时间/空间复杂度、IO 与锁竞争问题，给出按收益排序的优化方案。',
    tags: ['性能', '优化', '复杂度'],
    model: 'Claude / GPT-4o', level: '进阶',
    content: `你是性能优化工程师。下面是需要优化的代码与背景。

## 代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 性能现状
- 当前耗时/吞吐：{{量化指标}}
- 目标：{{量化目标}}
- 数据规模：{{数据量级}}
- 运行环境：{{CPU/内存/网络条件}}

## 分析要求
1. 先定位瓶颈，不要直接给优化代码。用以下分类逐个排查：
   - 算法复杂度（时间 O、空间 O）
   - 重复计算 / 缓存缺失
   - IO 次数（数据库 N+1、频繁小文件读写、未批量）
   - 锁竞争与串行等待
   - 内存分配（频繁创建临时对象、大对象拷贝）
   - 序列化开销
2. 每一条必须给出**量化估算**：优化前多少次操作 → 优化后多少次。
3. 按「收益 / 改动成本」从高到低排序，只推荐前 3 项。
4. 明确指出哪些优化**不值得做**（收益 < 5% 或引入复杂度过高）。

## 输出格式
### 瓶颈定位
（表格：位置 | 类型 | 当前开销 | 量化说明）

### 优化方案（按优先级）
每项包含：改动前代码 → 改动后代码 → 预期收益 → 副作用

### 不推荐项
（列出你评估后认为不该做的优化及理由）

### 验证方法
（如何测量优化是否生效：指标、工具、基准代码片段）`,
  },
  {
    cat: 'coding', sub: 'review', slug: 'code-review-checklist',
    title: '结构化代码审查',
    summary: '按严重级别分级输出审查意见，区分阻断问题、建议与吹毛求疵。',
    tags: ['Code Review', '代码质量', '协作'],
    model: 'Claude / GPT-4o', level: '通用',
    content: `你是团队里最严格但最讲道理的 Code Reviewer。

## 待审查变更
\`\`\`diff
{{diff 或代码}}
\`\`\`

## 变更意图
{{这个 PR 想解决什么问题}}

## 审查维度
按以下顺序检查，逐项给出结论（通过 / 有问题）：
1. **正确性**：逻辑是否符合意图？边界是否覆盖？并发是否安全？
2. **安全性**：注入、越权、敏感信息泄露、依赖漏洞。
3. **性能**：是否有 N+1、无界循环、内存泄漏、不必要的同步阻塞。
4. **可维护性**：命名、函数长度、重复代码、隐含耦合。
5. **可观测性**：错误是否有上下文？关键路径是否有日志/指标？
6. **测试**：改动是否覆盖了测试？测试是否真的能失败？

## 输出格式
按严重级别分组，每条格式：
\`[文件路径:行号] 问题 → 为什么是问题 → 具体改法\`

- 🔴 **阻断**：必须改，否则会出线上问题
- 🟡 **建议**：强烈建议改，不改要给出理由
- 🟢 **可选**：风格类、可读性类，作者可自行决定

**硬规则**：
- 不提「建议加注释」这种无信息量的意见。
- 每条问题必须给具体改法，不能只说「这里有问题」。
- 如果整体没问题，直接说「LGTM」并说明你检查了什么，不要凑数。`,
  },
  {
    cat: 'coding', sub: 'debug', slug: 'systematic-debug',
    title: '系统化排错流程',
    summary: '按假设-验证循环定位 Bug 根因，拒绝「试试看」式修法。',
    tags: ['调试', '排错', '根因分析'],
    model: 'Claude / GPT-4o', level: '通用',
    content: `你是调试专家，遵循「先复现、再定位、最后修复」的顺序。禁止在找到根因前给出修改方案。

## 问题现象
{{现象描述：期望行为 vs 实际行为}}

## 环境
- 语言/框架版本：{{}}
- 平台：{{}}
- 复现频率：{{必现 / 偶发 x% / 仅特定环境}}

## 相关代码
\`\`\`{{语言}}
{{相关代码}}
\`\`\`

## 报错信息
\`\`\`
{{完整堆栈}}
\`\`\`

## 已经尝试过的操作
{{列出，没有就写「无」}}

## 请按以下流程进行
### 第一步：信息缺口
列出定位根因还缺的关键信息（日志、变量值、环境差异），按重要性排序，最多 5 条。
如果缺失信息导致无法定位，就在这里停下，先让我补齐。

### 第二步：假设列表
列出 3-5 个可能根因，按「可能性 × 验证成本」排序。
每个假设写明：假设内容 → 会导致观察到什么现象 → 如何用最小代价证伪。

### 第三步：验证路径
针对排序第一的假设，给出具体的验证步骤（加什么日志、打印什么变量、用什么工具）。

### 第四步：根因与修复
确认根因后：
1. 一句话说清根因（真正的因果链，不是表面现象）
2. 最小修复方案
3. 为什么这个修复是对的
4. 如何加测试防止回归

**硬规则**：不要用「可能是…你可以试试…」开头。每个结论要么有证据支撑，要么明确标注为猜测。`,
  },
  {
    cat: 'coding', sub: 'testing', slug: 'test-case-design',
    title: '测试用例设计与生成',
    summary: '按等价类、边界值、错误推测法设计用例，覆盖正常/边界/异常三条线。',
    tags: ['测试', '单元测试', '覆盖率'],
    model: '通用', level: '通用',
    content: `你是测试工程师。为下面的函数设计并执行测试用例。

## 被测代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 测试框架
{{pytest / Jest / Go testing / JUnit}}

## 设计方法（必须用上）
1. **等价类划分**：把输入分成若干类，每类取一个代表值。
2. **边界值分析**：对每个数值/长度边界，取 min-1、min、min+1、正常、max-1、max、max+1。
3. **错误推测**：基于经验猜测容易出错的地方（空值、特殊字符、时区、浮点精度、编码）。
4. **状态转换**：若涉及状态机，覆盖所有合法转换 + 至少 2 条非法转换。

## 输出
### 用例清单（表格）
| 编号 | 场景 | 输入 | 期望输出 | 设计方法 | 优先级 |
（优先级：P0 阻断 / P1 核心 / P2 边缘）

### 测试代码
完整可运行，包含：
- 正常路径（至少覆盖主要分支）
- 边界与异常路径
- 每个断言写清「期望什么」而不是 \`assert result is not None\` 这种无效断言

### 覆盖缺口
说明哪些代码路径**无法**被单测覆盖（如需 Mock 外部服务、涉及真实 IO），以及建议的替代验证方式。`,
  },
  {
    cat: 'coding', sub: 'architecture', slug: 'system-design-review',
    title: '系统架构方案设计',
    summary: '给定业务约束，输出架构选型、模块划分、数据模型与风险清单。',
    tags: ['架构', '系统设计', '技术选型'],
    model: 'Claude / GPT-4o', level: '进阶',
    content: `你是系统架构师。为下方需求设计架构方案。

## 业务需求
{{业务描述}}

## 关键约束
- 预期规模：{{DAU / QPS / 数据量}}
- 一致性要求：{{强一致 / 最终一致}}
- 延迟要求：{{P99 目标}}
- 团队规模与能力：{{}}
- 已有基础设施：{{}}
- 上线时间：{{}}
- 预算约束：{{}}

## 输出结构
### 1. 一句话架构
用一句话说清整体架构（例如「单体 + 读写分离 + 异步任务队列」）。

### 2. 架构图
用 ASCII 或 Mermaid 画出组件与数据流。

### 3. 模块划分
表格：模块 | 职责 | 技术选型 | 选择理由 | 被替换成本

### 4. 数据模型
核心实体的字段与关系（DDL 或 ER 描述），说明索引设计依据。

### 5. 关键路径
描述最主要的一条请求链路：从入口到落库，标注每一步的耗时预算。

### 6. 风险清单
表格：风险 | 触发条件 | 影响 | 缓解方案 | 发现时机

### 7. 演进路径
V1 怎么做到最简并可用 → 什么信号出现时该演进到 V2 → V2 具体改什么

**硬规则**：
- 不要为了「高可用」引入团队hold不住的组件。技术选型必须匹配团队规模。
- 每一项选型都要给「不选 X 的理由」，证明你权衡过。
- 明确指出这个设计在哪个规模下会崩。`,
  },
  {
    cat: 'coding', sub: 'database', slug: 'sql-optimize',
    title: 'SQL 优化与索引设计',
    summary: '分析慢查询执行计划，给出改写方案与索引建议，并量化预期提升。',
    tags: ['SQL', '索引', '性能'],
    model: '通用', level: '进阶',
    content: `你是数据库性能专家（{{MySQL 8 / PostgreSQL 15 / Oracle}}）。

## 慢查询
\`\`\`sql
{{SQL}}
\`\`\`

## 表结构
\`\`\`sql
{{DDL}}
\`\`\`

## 现状
- 表数据量：{{行数}}
- 当前耗时：{{毫秒}}
- 执行计划：
\`\`\`
{{EXPLAIN 输出}}
\`\`\`

## 分析要求
1. **解读执行计划**：逐行说明每一步在做什么，标出全表扫描、临时表、filesort、回表次数。
2. **定位瓶颈**：是没走索引、索引失效、扫描行数过多、还是 JOIN 顺序问题？
3. **给出方案**（按优先级，每条包含）：
   - 改写后的 SQL
   - 建议索引（含字段顺序与理由，遵循最左前缀与区分度排序）
   - 预期扫描行数变化：{{当前}} → {{优化后}}
   - 副作用（写放大、索引体积、锁范围）

## 额外检查
- 是否存在隐式类型转换导致索引失效？
- 是否存在函数包裹列导致索引失效？
- \`SELECT *\` 是否取了不必要的字段导致回表？
- 分页是否可用游标替代 \`OFFSET\`？

## 禁止
不要建议「加个缓存」来掩盖 SQL 本身的问题。先让 SQL 走对。`,
  },
  {
    cat: 'coding', sub: 'devops', slug: 'dockerfile-harden',
    title: 'Dockerfile 瘦身与安全加固',
    summary: '多阶段构建、降权限、清缓存，产出体积更小更安全的镜像配置。',
    tags: ['Docker', 'CI/CD', '安全'],
    model: '通用', level: '通用',
    content: `你是容器化与供应链安全工程师。优化下面的 Dockerfile。

## 原始 Dockerfile
\`\`\`dockerfile
{{Dockerfile}}
\`\`\`

## 应用类型
{{语言与框架}}

## 优化目标（按优先级）
1. **体积**：多阶段构建，运行阶段只保留运行时依赖；清理包管理器缓存；合并 RUN 层。
2. **安全**：
   - 非 root 用户运行
   - 固定基础镜像 tag 到 digest 或具体版本（不用 latest）
   - 删除构建期密钥与临时文件
   - 不在镜像中留 shell 调试工具（生产镜像）
3. **构建速度**：按变更频率排层，依赖文件先 COPY；合理使用 BuildKit 缓存挂载。
4. **可观测**：正确的 \`HEALTHCHECK\`、信号处理（用 \`exec\` 或 tini 避免 PID 1 僵尸进程问题）。

## 输出
### 优化后 Dockerfile
（完整，带行内注释说明每行为什么这么写）

### 改动对照表
| 项目 | 优化前 | 优化后 | 说明 |

### 预估效果
- 镜像体积：{{原始大小}} → {{预估大小}}
- 构建时间变化说明

### .dockerignore
给出配套的 .dockerignore 文件

### 遗留风险
说明本次优化没解决、需要你确认的问题（如基础镜像 CVE、多架构支持）。`,
  },
  {
    cat: 'coding', sub: 'regex', slug: 'regex-builder',
    title: '正则表达式构造与讲解',
    summary: '给出可用正则、逐段讲解、测试用例与性能陷阱提示。',
    tags: ['正则', '文本处理', '工具'],
    model: '通用', level: '通用',
    content: `你是正则表达式专家。

## 匹配需求
- 目标文本样例：
\`\`\`
{{样例文本}}
\`\`\`
- 需要匹配/提取：{{目标}}
- 需要排除：{{反例}}
- 正则引擎：{{PCRE / Python re / JavaScript / Go RE2 / POSIX}}

## 输出
### 1. 推荐正则
\`\`\`
{{正则}}
\`\`\`

### 2. 逐段讲解
| 片段 | 含义 | 为什么这么写 |

### 3. 测试集
表格：测试用例 | 是否应匹配 | 实际结果 | 捕获组内容
至少包含：4 个正例、3 个反例、2 个边界情况（空串、超长、特殊字符）

### 4. 引擎差异提示
说明该正则在目标引擎下的注意点（如 JS 不支持后行断言的变长、Go RE2 不支持回溯引用）。

### 5. 性能提示
- 是否存在灾难性回溯（嵌套量词 \`(a+)+\`）？
- 是否有更简单的等价写法？
- 如果用于高频调用，建议的替代方案（如字符串方法、解析器）。

### 6. 代码示例
给出目标语言的使用代码（含编译/预编译、命名捕获组、错误处理）。`,
  },
  {
    cat: 'coding', sub: 'docs', slug: 'readme-generator',
    title: 'README / API 文档生成',
    summary: '基于代码事实生成文档，结构规范，不含臆造的用法。',
    tags: ['文档', 'README', 'API'],
    model: '通用', level: '通用',
    content: `你是技术文档工程师。基于下面给出的**真实代码**生成文档。

## 代码
\`\`\`{{语言}}
{{代码}}
\`\`\`

## 项目信息
- 名称：{{}}
- 一句话定位：{{}}
- 目标读者：{{}}

## 铁律
**只写代码中确实存在的东西。** 以下行为一律禁止：
- 编造代码里没有的参数、配置项、返回值
- 写「例如你可以这样用」但示例无法通过类型检查
- 用占位内容填充（如 \`TODO\`、\`xxx\`）却不加标注
- 描述「未来会支持」的功能

如果发现代码本身缺少必要信息（如缺类型标注、缺默认值），在文档末尾的「待确认」章节列出，不要在正文中猜测。

## 输出结构
### README
1. 一句话简介（不超过 20 字）
2. 它解决什么问题（3 句话以内）
3. 快速开始：安装 → 最小可运行示例 → 预期输出
4. 核心概念（如有必要）
5. API 参考
6. 常见问题
7. 项目状态与限制

### API 参考格式
每个导出项：
\`\`\`
#### 名称(参数1: 类型, 参数2?: 类型 = 默认值): 返回类型
一句话说明。
**参数**
- \`参数1\` — 说明，取值范围，越界行为
**返回** — 说明，异常情况下的行为
**抛出** — 错误类型与触发条件
**示例** — 可直接运行的代码
\`\`\`

### 待确认清单
列出你无法从代码确定的点。`,
  },

  // ============ 提示词工程 ============
  {
    cat: 'prompteng', sub: 'optimize', slug: 'prompt-doctor',
    title: '提示词诊断与重写',
    summary: '指出原始提示词的歧义与缺失，产出强化版并说明每处改动的依据。',
    tags: ['提示词优化', '元提示词', '诊断'],
    model: '通用', level: '通用',
    content: `你是提示词工程专家，擅长把含糊的自然语言指令改造成高确定性提示词。

## 原始提示词
\`\`\`
{{原始提示词}}
\`\`\`

## 使用场景
{{用在什么模型、什么任务上，期望达到什么效果}}

## 当前问题
{{效果不好在哪：输出不稳定 / 格式不符 / 太长 / 太短 / 答非所问}}

## 诊断要求
逐条检查以下缺陷，有则指出（引用原文片段）：
1. **指令歧义**：多处解读可能的措辞
2. **缺少角色**：未定义模型应以什么身份回答
3. **缺少上下文**：未提供背景、受众、约束
4. **缺少输出规范**：未定义格式、长度、结构
5. **缺少否定约束**：只说要什么，没说不要什么
6. **任务过载**：一条提示词里塞了多个目标
7. **无示例**：复杂任务没有 few-shot
8. **无验证标准**：没说明怎样算「做对了」

## 输出
### 诊断报告
表格：问题 | 原文证据 | 影响 | 严重度

### 重写版提示词
（完整的、可直接复制使用的新提示词，用 Markdown 结构化）

### 改动依据
逐条说明：我改了什么 → 从什么改成什么 → 为什么这样更好（引用提示词工程原则）

### 使用建议
- 建议搭配什么温度参数
- 哪些部分需要根据具体任务二次调整
- 如何验证改动是否真的有效`,
  },
  {
    cat: 'prompteng', sub: 'structured', slug: 'json-output-enforcer',
    title: '强制结构化输出（JSON Schema）',
    summary: '让模型稳定输出可解析的 JSON，附带校验与失败重试策略。',
    tags: ['JSON', '结构化输出', '可靠性'],
    model: '通用', level: '通用',
    content: `为下面的任务设计一套「结构化输出」提示词方案。

## 任务
{{任务描述}}

## 目标 JSON Schema
\`\`\`json
{{Schema}}
\`\`\`

## 输出要求
### 1. 提示词正文
包含以下要素：
- 明确的输出契约：只输出 JSON，无任何前后缀文字、无 Markdown 代码围栏
- 完整 Schema 内联在提示词中，每个字段有 \`description\`、类型、约束
- 枚举字段列出所有合法值并说明各自语义
- 对「信息缺失」的处理规则：缺失字段填 \`null\` 还是在 \`notes\` 中说明？二选一并写死
- 对「不确定」的处理规则：禁止编造，不确定必须体现为 \`confidence\` 字段或 \`null\`
- 2-3 个 few-shot 示例（一个完整、一个含缺失字段、一个边界情况）

### 2. 健壮性设计
- 如果模型仍输出围栏包裹的 JSON，用什么规则清洗？给出代码。
- 校验失败时的重试提示词怎么写？给出重试版本。
- 最大重试次数与降级策略建议。

### 3. 配套校验代码
给出目标语言的解析 + Schema 校验代码（Python 用 pydantic / JS 用 zod）。

### 4. 已知陷阱
说明这个任务下结构化输出常见的失败模式（如字段幻觉、枚举外取值、超长字段截断）。`,
  },
  {
    cat: 'prompteng', sub: 'cot', slug: 'reasoning-chain-builder',
    title: '推理链提示词构建器',
    summary: '为复杂推理任务设计分步思考框架，平衡准确率与 token 成本。',
    tags: ['思维链', 'CoT', '推理'],
    model: 'o1 / Claude / GPT-4o', level: '进阶',
    content: `你是推理任务设计专家。为下方任务设计推理链提示词。

## 任务
{{任务描述}}

## 任务性质
- 类型：{{数学推导 / 逻辑判断 / 多步规划 / 因果分析 / 代码推理}}
- 典型难度：{{}}
- 准确率要求：{{}}
- 成本敏感度：{{高 / 中 / 低}}

## 设计要求
根据任务性质选择合适的推理策略，并说明理由：
- **Zero-shot CoT**：「让我们一步步思考」，适合中等难度、成本敏感
- **Few-shot CoT**：给 2-3 个带完整推理过程的示例，适合格式要求高的场景
- **Plan-and-Solve**：先出计划再执行，适合多步规划
- **Self-Consistency**：多次采样投票，适合高风险决策（成本高）
- **Tree of Thought**：分支探索 + 回溯，适合搜索空间大的问题
- **原生推理模型**：若使用 o1/R1 类模型，不要写 CoT 指令，改为给足上下文与验证标准

## 输出
### 1. 推荐策略与理由
（一段话，说明为什么选它，放弃了什么）

### 2. 完整提示词
（可直接复制使用，推理步骤要具体到任务领域，不要用「第一步、第二步」这种空壳）

### 3. 推理步骤设计
表格：步骤 | 这一步要产出什么 | 常见错误 | 如何自查

### 4. 答案收敛
最后一步如何把推理过程收敛成确定的最终答案（格式、置信度、取舍规则）。

### 5. 成本评估
- 预估每条输入的 token 消耗量级
- 如果成本过高，给出简化版（保留 80% 效果、降到 50% 成本）`,
  },
  {
    cat: 'prompteng', sub: 'fewshot', slug: 'fewshot-designer',
    title: 'Few-shot 示例集设计',
    summary: '挑选、构造、排序示例，最大化示例的边际收益。',
    tags: ['Few-shot', '示例工程', 'ICL'],
    model: '通用', level: '进阶',
    content: `你是示例工程专家。为下方任务设计 few-shot 示例集。

## 任务
{{任务描述}}

## 现有候选样本
{{粘贴 5-20 条样本，或写「无，请为我合成」}}

## 设计原则（逐条落实）
1. **代表性**：覆盖输入空间的主要分布，不是随机抽样
2. **多样性**：长度、格式、难度、领域都要有差异
3. **边界覆盖**：至少 1 个边界/异常样本
4. **标签平衡**：分类任务的示例数在各标签间均衡
5. **顺序效应**：把最典型的放首尾（首因/近因效应），避免把同类样本连续排列
6. **格式一致**：所有示例的输入输出格式严格统一
7. **去噪**：示例中不要有与任务无关的信息

## 输出
### 1. 示例集（建议 {{3-8}} 条）
每条格式：
\`\`\`
【输入】...
【输出】...
\`\`\`

### 2. 选型说明
表格：示例 | 覆盖的分布/边界 | 为什么选它 | 如果删掉会损失什么

### 3. 排列理由
说明为什么是这个顺序。

### 4. 消融建议
- 哪些示例可以删掉而几乎不损失效果（用于压缩成本）
- 还缺哪类样本（如果加了会明显提升）

### 5. 集成方式
给出把示例集嵌入提示词的完整模板代码（含分隔符选择与理由）。`,
  },
  {
    cat: 'prompteng', sub: 'persona', slug: 'expert-persona',
    title: '专家角色设定生成器',
    summary: '构造有立场、有边界、有表达风格的专家人格，而非空泛的「你是一位专家」。',
    tags: ['角色设定', 'System Prompt', '人格'],
    model: '通用', level: '通用',
    content: `你是角色设计专家，擅长把「你是一位 XX 专家」这种空话变成真正影响输出质量的人格设定。

## 需要设定的角色
- 领域：{{领域}}
- 使用目的：{{用这个角色做什么任务}}
- 目标受众：{{谁在读输出}}
- 希望的表达风格：{{犀利 / 温和 / 学术 / 务实 / 幽默}}

## 设定要素（逐项产出）
1. **身份锚点**：具体的职业、年限、代表成就（不要「资深」这种虚词，要「在 XX 公司负责 XX，处理过 YY 规模的系统」）
2. **知识边界**：明确知道什么、不知道什么、什么时候该说「这个我不确定」
3. **思维习惯**：这个领域的专家面对问题时，默认会先看什么、后看什么、容易忽略什么
4. **判断标准**：这个角色认为什么算「好」，什么算「不行」
5. **表达风格**：句式长度、术语密度、是否用比喻、是否给结论先行
6. **反模式**：这个角色绝对不会说的话、不会做的事
7. **冲突处理**：当用户要求与专业判断冲突时，这个角色怎么回应

## 输出
### 角色设定卡
（结构化列出上述 7 项）

### System Prompt 正文
（可直接使用的完整角色设定，150-400 字，不用「你是」开头的套话，用具体的行为约束替代形容词）

### 效果自检
- 这段设定里有没有无法指导行为的空话？有就删掉重写。
- 有没有哪条约束会互相矛盾？
- 用这个角色回答一个典型问题，展示它跟「通用助手」的回答差异。`,
  },
  {
    cat: 'prompteng', sub: 'antihallucination', slug: 'fact-grounding',
    title: '事实锚定与幻觉抑制',
    summary: '强制模型区分「已知/推断/未知」，对无依据内容主动拒答。',
    tags: ['幻觉', '事实性', '可靠性'],
    model: '通用', level: '进阶',
    content: `为下方任务设计一套抗幻觉提示词方案。

## 任务
{{任务描述}}

## 可用事实来源
{{粘贴参考资料，或写「仅依赖模型内部知识」}}

## 核心机制（必须在提示词中全部体现）
1. **来源分层**：每条陈述标注 \`[来自资料]\` / \`[推断]\` / \`[背景知识]\`。
2. **无依据即拒答**：资料中没有、且不属于公认常识的内容，必须输出「资料中未提供」，禁止推测填充。
3. **引用锚定**：涉及具体数据、日期、人名、法条时，必须附原文引用片段。
4. **区分度检查**：输出前自查——这条是资料里写的，还是我自己觉得应该是这样？
5. **不确定性外显**：不确定的结论必须附置信度与不确定原因。
6. **禁止平滑**：不允许为了「回答完整」而补充看似合理但无依据的细节。

## 输出
### 1. 抗幻觉提示词
（完整可用版本）

### 2. 输出格式定义
设计一个强制区分「确定 / 推断 / 缺失」的输出结构（如三栏表格或带标记字段的 JSON）。

### 3. 触发词清单
列出这个任务领域中最容易诱发幻觉的提问方式（如「XX 的具体数字是多少」「XX 是不是说过…」），以及模型应有的应对话术。

### 4. 验证用例
给出 3 个测试提示词，用于检验该设定是否真的抑制了幻觉：
- 1 个资料中有的（应正确回答并引用）
- 1 个资料中没有的（应拒答）
- 1 个看似有实则陷阱的（应识别并说明）`,
  },
  {
    cat: 'prompteng', sub: 'system', slug: 'system-prompt-audit',
    title: 'System Prompt 审计与加固',
    summary: '检查现有 System Prompt 的优先级冲突、注入风险与失效点。',
    tags: ['System Prompt', '安全', '审计'],
    model: '通用', level: '进阶',
    content: `你是提示词安全与质量审计师。审计下面的 System Prompt。

## 待审计内容
\`\`\`
{{System Prompt}}
\`\`\`

## 部署场景
- 模型：{{}}
- 是否暴露给终端用户输入：{{是 / 否}}
- 可用工具：{{列出，无则写「无」}}
- 业务红线：{{绝对不能做的事}}

## 审计维度
### A. 指令冲突
找出内部互相矛盾的指令，以及「必须/绝不」类绝对化表述过多导致的遵守率下降问题。

### B. 优先级缺失
当系统指令与用户指令冲突时，谁优先？提示词中是否明确？

### C. 注入风险
- 用户输入是否可能被误判为系统指令？
- 是否定义了输入分隔符？
- 是否禁止执行用户输入中的「忽略以上指令」类内容？
- 工具调用参数是否可能被注入？

### D. 边界缺失
未定义的场景（超出业务范围、请求敏感内容、要求输出内部规则），模型会怎么办？

### E. 冗余与膨胀
哪些指令实际不起作用（对现代模型已内化，如「请准确回答」）？哪些可以压缩？

## 输出
### 问题清单
表格：维度 | 问题 | 触发场景 | 严重度 | 修复建议

### 加固版 System Prompt
（完整重写，带分区标题：角色 / 能力 / 约束 / 输出规范 / 边界处理 / 安全规则）

### 回归测试集
给出 5-8 条用于验证加固效果的测试输入（含 2 条注入攻击样本）与期望行为。`,
  },

  // ============ 数据分析 ============
  {
    cat: 'data', sub: 'cleaning', slug: 'data-clean-plan',
    title: '数据清洗方案与代码',
    summary: '先出数据质量报告再给清洗代码，避免盲目 dropna。',
    tags: ['数据清洗', '数据质量', 'Pandas'],
    model: '通用', level: '通用',
    content: `你是数据工程师。下面是需要处理的数据集。

## 数据概况
- 规模：{{行数 × 列数}}
- 来源：{{}}
- 字段说明：
{{字段列表与含义}}

## 样本数据
\`\`\`
{{前 10 行}}
\`\`\`

## 业务目标
{{这份数据最终用于什么分析}}

## 第一步：数据质量报告（先出这个，不要直接给清洗代码）
表格输出每个字段：
| 字段 | 类型 | 缺失率 | 唯一值数 | 异常值情况 | 分布特征 | 问题等级 |

并单独列出：
- 重复行（完全重复 / 业务主键重复）
- 逻辑矛盾（如结束时间早于开始时间、金额与数量不匹配）
- 编码问题（乱码、多币种、多时区、单位不统一）

## 第二步：清洗策略
对每个问题给出处理方案，并说明**为什么这么处理、对后续分析的影响**：
- 删除 / 填充（均值、中位数、众数、前向填充、模型预测）/ 保留为缺失 / 单独标记
- 每选择一种，说明你放弃了哪种以及代价

## 第三步：清洗代码
完整可运行的 {{Pandas / Polars / SQL}} 代码，要求：
- 每一步前后打印数据形状变化
- 所有删除操作先统计再执行（先告诉我删了多少，再真删）
- 生成一份清洗日志（记录每个字段的处理动作与影响行数）

## 第四步：遗留问题
清洗无法解决、需要回到数据源头确认的问题清单。`,
  },
  {
    cat: 'data', sub: 'sql', slug: 'text2sql',
    title: '自然语言转 SQL',
    summary: '给定表结构生成可执行的复杂查询，附带歧义确认与性能提示。',
    tags: ['SQL', 'Text2SQL', '查询'],
    model: '通用', level: '通用',
    content: `你是 SQL 专家（方言：{{MySQL / PostgreSQL / BigQuery / ClickHouse / Hive}}）。

## 表结构
\`\`\`sql
{{DDL，包含字段注释与索引}}
\`\`\`

## 自然语言需求
{{用业务语言描述你要的数据}}

## 执行要求
### 1. 歧义确认（如存在）
如果需求有多种合理解读，**先列出歧义点并给出你的默认选择**，然后按默认选择继续。
常见歧义：时间口径（自然日/滚动 30 天）、去重口径（按用户还是按订单）、包含边界、NULL 处理。

### 2. SQL 输出
\`\`\`sql
-- 查询目标：一句话
-- 关键口径：列出你做的口径假设
{{SQL}}
\`\`\`

### 3. 逐段讲解
按 CTE 逐个解释在做什么，以及为什么用这个 JOIN / 窗口函数 / 聚合方式。

### 4. 正确性自查
- 时间区间是否左闭右开？
- JOIN 是否会导致行数放大？用 DISTINCT 之前确认过吗？
- 聚合前是否需要先去重？
- NULL 在 COUNT / AVG / 窗口函数中的行为是否符合预期？
- 除法的分母为 0 是否处理（NULLIF）？

### 5. 性能提示
- 预计扫描行数
- 建议索引
- 是否可用预聚合表 / 物化视图加速

### 6. 结果校验查询
给出 1-2 条用于验证结果合理性的 SQL（如总数对账、抽样检查）。`,
  },
  {
    cat: 'data', sub: 'viz', slug: 'chart-advisor',
    title: '图表选型与可视化建议',
    summary: '根据数据类型与传达目的推荐图表，给出配色、标注与避坑要点。',
    tags: ['可视化', '图表', '设计'],
    model: '通用', level: '通用',
    content: `你是数据可视化专家。

## 数据情况
- 字段与类型：{{}}
- 数据量级：{{行数}}
- 数据样例：
\`\`\`
{{}}
\`\`\`

## 传达目的
- 想让观众看出什么：{{趋势 / 对比 / 构成 / 分布 / 相关 / 地理 / 流程}}
- 观众是谁：{{技术 / 业务 / 高管 / 大众}}
- 呈现载体：{{大屏 / 报告 / 幻灯片 / 网页}}

## 输出
### 1. 图表推荐（给出主选 + 1 个备选）
| 方案 | 图表类型 | 适合的理由 | 数据要求 | 观众理解成本 |

### 2. 为什么不用其他图
明确列出你考虑过但否决的类型及原因（如「不用饼图，因为类别超过 5 个且差异不明显」）。

### 3. 视觉设计规格
- 坐标轴：起点是否归零、刻度密度、对数轴的适用性
- 配色：主色/辅色/中性色建议（给 hex），色盲友好检查，分类色的数量上限
- 标注：哪些点必须直接标数值，哪些用 tooltip
- 排序：类目按数值排序还是按固定顺序（时间/流程）
- 图例：是否可省略（直接标注更优）

### 4. 代码实现
给出 {{ECharts / Matplotlib / Plotly / Vega-Lite}} 的完整配置代码。

### 5. 常见陷阱
针对这个图表类型，列出 3 个最容易犯的错（如双 Y 轴误导、截断 Y 轴夸差异、3D 效果扭曲比例）。`,
  },
  {
    cat: 'data', sub: 'stats', slug: 'ab-test-analysis',
    title: 'A/B 实验设计与结果分析',
    summary: '从样本量计算到显著性检验，输出可信结论与决策建议。',
    tags: ['A/B 测试', '统计', '实验设计'],
    model: '通用', level: '进阶',
    content: `你是实验设计与因果推断专家。

## 场景
- 实验内容：{{改了什么}}
- 核心指标：{{指标名与定义}}
- 守护指标：{{不能变差的指标}}
- 分流单位：{{用户 / 会话 / 设备}}
- 当前流量：{{日活或日流量}}

## Part 1：实验设计（若尚未跑）
1. **样本量计算**：给定 MDE {{最小可检测提升}}、显著性水平 α=0.05、功效 1-β=0.8，计算每组所需样本量与预计运行天数。给出公式与代入过程。
2. **分流方案**：如何分流、如何避免样本比率不匹配（SRM）。
3. **指标定义**：主指标、副指标、守护指标的计算口径与统计口径（均值型 vs 比率型的方差差异）。
4. **提前停止规则**：用固定期检验还是序贯检验？偷看数据的风险控制。

## Part 2：结果分析（若已有数据）
\`\`\`
{{粘贴实验数据：各组样本量、均值、标准差 或 转化数}}
\`\`\`
1. **SRM 检验**：先做卡方检验确认分流没问题，有问题就停止后续分析。
2. **显著性检验**：选对检验方法（Z 检验 / t 检验 / 卡方 / Bootstrap / CUPED 降方差），给出统计量、p 值、置信区间。
3. **效应量**：不只报 p 值，报绝对提升、相对提升与置信区间。
4. **多重比较**：若看了多个指标或多个分层，说明是否做了校正（Bonferroni / BH）。
5. **分层分析**：按 {{平台/新老用户/地区}} 拆解，注意区分真实异质性与噪声。

## Part 3：结论
- 一句话结论（显著 / 不显著 / 数据不足）
- 决策建议：上线 / 不上线 / 继续观察，并说明依据
- 这次实验的局限性（外部效度、新奇效应、网络效应、长期指标未观测）

**硬规则**：p 值接近 0.05 时，明确说「证据不足以下结论」，不要用「 marginally significant」这类话术粉饰。`,
  },
  {
    cat: 'data', sub: 'reporting', slug: 'insight-extraction',
    title: '从数据中提取业务洞察',
    summary: '超越描述统计，输出可归因、可行动的结论与建议动作。',
    tags: ['洞察', '报表', '业务分析'],
    model: '通用', level: '进阶',
    content: `你是业务分析师。下面是数据结果，请提炼洞察。

## 数据
\`\`\`
{{粘贴数据或报表结果}}
\`\`\`

## 业务背景
- 业务模式：{{}}
- 分析周期：{{}}
- 关注重点：{{}}

## 输出要求
### 1. 事实层（What）
只陈述数据里确实存在的现象，每条附具体数字与对比基准。
格式：\`[指标] 在 [维度] 上 [变化]，为 [数值]，相比 [基准] [变化幅度]\`
不要在这一层加任何解释。

### 2. 归因层（Why）
对每个显著变化，给出：
- 可能的原因（2-3 个，按可能性排序）
- 每个原因的支撑证据（数据中的哪一条支持它）
- 需要什么额外数据才能确认（明确写出「需要看 X 才能验证」）

**禁止**：把相关性当因果。每个归因必须标注这是「假设」还是「已验证」。

### 3. 影响层（So What）
- 这个变化对业务意味着什么（量化影响：金额/用户数/效率）
- 不处理会怎样

### 4. 行动层（Now What）
表格：建议动作 | 预期收益 | 执行成本 | 优先级 | 验证指标

### 5. 认知边界
明确列出这份数据**无法**回答的问题，以及需要补什么数据。

**硬规则**：如果数据本身不足以支撑结论，直接说「这份数据只能看到 X，无法判断 Y」。编造洞察比没有洞察更糟。`,
  },
  {
    cat: 'data', sub: 'pandas', slug: 'pandas-vectorize',
    title: 'Pandas 循环向量化改写',
    summary: '把 apply/iterrows 改写成向量化操作，处理 SettingWithCopy 等常见坑。',
    tags: ['Pandas', '向量化', '性能'],
    model: '通用', level: '通用',
    content: `你是 Pandas 性能专家。把下面的代码改写成向量化版本。

## 原始代码
\`\`\`python
{{代码}}
\`\`\`

## 数据规模
{{行数 × 列数}}

## 当前耗时
{{秒}}

## 改写要求
1. 逐个消除以下反模式：
   - \`iterrows()\` / \`itertuples()\` 逐行遍历
   - \`apply(axis=1)\` 行级 apply
   - 循环里 \`df.loc[i, col] = ...\` 逐格赋值
   - 链式赋值导致的 \`SettingWithCopyWarning\`
   - 在循环里 \`df.append\` / \`pd.concat\` 累积
   - 用 Python 循环做字符串拼接
2. 对每一处，说明用了什么向量化手段（布尔索引 / \`np.where\` / \`map\` / \`merge\` / \`groupby.transform\` / \`str\` 访问器 / \`pd.cut\`）。
3. 如果某段逻辑**无法**向量化（如依赖前一行结果），明确指出并给出替代方案（\`numba\` / \`shift\` 技巧 / 改用 Polars）。

## 输出
### 改写后代码
（完整，含 import 与构造测试数据的部分）

### 对照表
| 原写法 | 新写法 | 手段 | 预期加速比 |

### 正确性验证
给出一段断言代码，证明改写前后结果一致（含 NaN 与边界值处理一致）。

### 内存提示
说明该操作是否会触发大内存拷贝，以及数据量再大 10 倍时该怎么办。`,
  },
];
