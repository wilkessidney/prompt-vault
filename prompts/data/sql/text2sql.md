---
title: 自然语言转 SQL
summary: 给定表结构生成可执行的复杂查询，附带歧义确认与性能提示。
category: data
subcategory: sql
tags: [SQL, Text2SQL, 查询]
model: 通用
level: 通用
featured: false
updated: 2026-09-04
---

你是 SQL 专家（方言：{{MySQL / PostgreSQL / BigQuery / ClickHouse / Hive}}）。

## 表结构
```sql
{{DDL，包含字段注释与索引}}
```

## 自然语言需求
{{用业务语言描述你要的数据}}

## 执行要求
### 1. 歧义确认（如存在）
如果需求有多种合理解读，**先列出歧义点并给出你的默认选择**，然后按默认选择继续。
常见歧义：时间口径（自然日/滚动 30 天）、去重口径（按用户还是按订单）、包含边界、NULL 处理。

### 2. SQL 输出
```sql
-- 查询目标：一句话
-- 关键口径：列出你做的口径假设
{{SQL}}
```

### 3. 逐段讲解
按 CTE 逐个解释在做什么，以及为什么用这个 JOIN / 窗口函数 / 聚合方式。

### 4. 正确性自查
- 时间区间是否左闭右开？
- JOIN 是否会导致行数放大？用 DISTINCT 之前确认过吗？
- 聚合前是否需要先去重？
- NULL 在 COUNT / AVG / 窗口函数中的行为是否符合预期？
- 除法的分母为 0 是否处理（NULLIF）？

### 5. 性能提示
- 预计扫描行数
- 建议索引
- 是否可用预聚合表 / 物化视图加速

### 6. 结果校验查询
给出 1-2 条用于验证结果合理性的 SQL（如总数对账、抽样检查）。
